# 文件整理规范（讨论稿，随讨论滚动更新）

> 状态：**讨论中**。用户口述整理规范，AI 记录成文；全部讨论完 → 转成规则融入文件整理工具。
> 关联文档：`nfo-plan-chat-hints.md`（AI 对话标签备注机制）、`/Users/leavehalf/Downloads/work/115/规则整理提案.md`（E/M/S 组提案）。
> 现状评估（用户 2026-09-11）：工具可用度约 40%，及格线都不到——根因是「之前没想清楚规范」；
> 本文档 = 先把规范想清楚，再转规则。

## 1. 总则：水印清洗内置（已拍板）

- **水印清洗不再作为独立规则**，而是**内置进所有改名规则**（同 M1 关联联动的方式）：
  任何规则改任何文件名之前，先跑一遍内置清洗管线——去掉各种广告、网站域名、站点标签。
- 规则整理页**保留一个「水印清洗」条目**，用于单独跑全量清洗。
- 实现含义：现 E 组的「域名前缀清洗 / 站点标签清洗」逻辑下沉为**公共前置管线**，
  所有规则（A/E/未来新组）默认携带；规则页 E 组里的两条是否保留独立入口待定。

## 2. 文件类型清单

用户列的 5 类：**视频、图片、文本、音频、压缩包**。
AI 建议补充：**字幕**（srt/ass/ssa/sub，跟随视频）、**NFO 元数据**（跟随视频）、
**其他/未知**（兜底类，不认识的先不动）。

## 3. 视频规范

视频细分为 6 类：**电影、电视剧、AV、动漫、单视频、小视频**（单视频/小视频的定义待用户补充）。

### 3.1 电影 · 命名（已拍板）

格式：`标题.原始标题.年份`

- 华语电影的原始标题**使用英文**；
- 没有原始标题、没有年份的，**直接使用标题**。

例：
- `让子弹飞.Let the Bullets Fly.2010.mkv`（华语，英文原始标题）
- `肖申克的救赎.The Shawshank Redemption.1994.mkv`
- `某独立短片.mkv`（无原始标题无年份 → 只用标题）

### 3.2 电影 · 文件夹归置（已拍板）

顶层分类：**系列 / 人物 / 地区 / 动画**。归置优先级：

1. **人物**：某个人的名气大于它的电影时（例：成龙、周星驰），相关电影放到该**人物**文件夹下；
2. **系列**：系列电影（例：加勒比海盗、速度与激情）放进对应**系列**文件夹；
3. **动画**：动画电影单独提出来；不属于人物也不属于系列的动画电影 → 放进**动画**；
4. 其余按**地区**归置。

**地区分类（已拍板）**：`华语 / 欧美 / 日韩 / 印泰 / 其他`（五档）。

### 3.3 剧集（原「电视剧」，已拍板）

> 类名统一定为「**剧集**」（用户 2026-09-11 定名）。

- **命名**：`标题.SxxExx`
- **分季规则**：多季 **且 总集数 > 100** 才建季文件夹（`Season 01` 这类）；其他情况一律不建季夹。
- **归置**：顶层按地区分类：`华语 / 欧美 / 日韩 / 其他`；**纪录片**是剧集的**特殊分类**，与「地区」**并列等级**（纪录片单独归置，不按地区分）。

### 3.4 动漫（已拍板）

- **命名**：跟剧集类似（`标题.SxxExx` 体系）
- **归置**：按地区分类：`日漫 / 国漫 / 欧美 / 其他`
- **两个特殊分类**：
  - **里番**：单独分类，不用多说；
  - **长漫**：类似哆啦A梦、海贼王这种**超过 200 集**以上的动漫，单独归入长漫。

### 3.5 AV（已拍板）

- **番号**：大写字母-数字（`ABC-123`）。
- **一夹一片**：每一部就是一个文件夹。
- **文件夹名 = `番号 标题`**（**空格**连接，如 `ABC-123 片名`）。
- **夹内标准 4 件套**：视频、nfo、剧照、海报；文件名规则：
  - 剧照 = `标题.fanart.jpg`
  - 海报 = `标题.poster.jpg`
- **多视频**（上下集 / CD1 CD2）：同一夹内多个视频，命名带 `.CD1` `.CD2`（如 `ABC-123.CD1.mp4`）。

### 3.6 单视频（已拍板，分两种）

1. **单视频电影**：主流播放器/刮削器识别不出数据，所以同样**一部影片一个文件夹**放置；
2. **网络杂片**：网上下载的，各种类型都可能有——工作室短片、个人拍摄等，**规范之后再说**。

### 3.7 小视频

文件大小较小的视频，处理方式类似单视频，**碰到了再说**。

## 4. 图片规范（主体已拍板，细节待确认）

图片分三类：**漫画 / 写真 / 网络杂图**。

- **小图清理**：太小的图片全部清理（没有重要性）；**阈值要好好斟酌**（待定）。
  实现注意：115 云端拿不到像素尺寸，只能按**文件大小（KB）**判断。

### 4.1 漫画

按 **短篇 / 长篇 / 合集 / PDF** 区分；PDF 虽不属于漫画，但有的漫画是 PDF 格式，**归入漫画**。
（短篇/长篇/合辑的定义用户认为好理解，按常识记录：短篇=单本完结、长篇=多章节/连载、合集=聚合）

- **短篇**：`标题` 文件夹，进入就是漫画图片；图片命名一般**三位数 `001~999`**。
- **长篇**分两种：
  - **有章节**：每个章节一个文件夹，里面图片也是三位数字命名；
  - **无章节**：人为分卷，**200 张一个文件夹**，夹名用两位数字：`00、02、04、06……`
    （00 = 001~200，02 = 201~400，**01 特意跳过**）。
    设计理由（用户拍板）：夹号 × 100 ≈ 总张数，一眼估算体量（如夹到 40 → 约 4000 余张）。
- **图片命名位数**：夹内总文件数 **≤999 → 三位数**；**>999 → 四位数**。
- **合集**：理解为一个系列、或某一个作者的作品、或某个主题的作品聚合。

### 4.2 写真（已拍板）

- **人物身份三分（不可混用）**：
  - **模特** = 专业从事写真拍摄的（有很多主题的写真）；
  - **女优 / 素人 / 演员** = 非专业写真人物，写真作品少——**三项合放一个文件夹**，夹名定为「**其他人物**」。
- **顶层五个维度（并列）**：`机构 / 模特 / 摄影师 / 类型 / 其他人物`。
- **归置优先级（已拍板）**：**人 > 机构 > 摄影师 > 类型**——
  认得出人 →（专业模特进「模特」，女优/素人/演员进「其他人物」）；
  认不出人 → 有机构名进「机构」→ 有摄影师署名进「摄影师」→ 都没有按「类型」兜底。
- **写真集（夹）命名格式（已拍板）**：`xxxx.xx.xx-主题[xxP.xxV.xxG]`
  - 日期 `xxxx.xx.xx`（点分隔，如 `2024.01.05`）；
  - `P` = 图片张数，`V` = 视频个数，`G` = 该文件夹的大小（GB）；
  - 例：`2024.01.05-海边旅拍[120P.2V.1.5G]`；
  - **无视频时省略 `0V`**（已拍板），只显示图片数量和大小，如 `2024.01.05-主题[120P.1.5G]`。
- **夹内结构（已拍板）**：有视频时，主题夹内分两个子文件夹，名为 **`P`**（放图片）和 **`V`**（放视频）；
  没有视频则不分子夹，图片直接放主题夹下。
- **图片命名（已拍板）**：`001~999` 三位数（与漫画短篇一致）。
- **视频命名（已拍板）**：看情况——文件名是乱码就改成 `001~999`；有意义的保留**视频主题名**。

### 4.3 网络杂图（与「网络杂片」合并讨论，规则待定稿）

- **用户口述（2026-09-11）**：网络杂图要和视频里「单视频第二种 · 网络杂片」**放一起说**——
  网上下载的内容往往是一个**压缩包**，里面既有视频又有图片，**实际是同一主题下的内容**；
  也有的包里只有视频，或只有图片。
- **含义**：网络杂图 / 网络杂片 / 压缩包三类应**统一成一套「网络下载主题包」规则**处理。
- **顶层夹定名「归档」（已拍板）**：网络杂图 / 网络杂片 / 压缩包三种包统一进「归档」顶层，按主题一包一夹。
- **归档下要二级分类（已拍板）**：下载内容多、主题夹会很多 → 「归档」下先分小类再放主题夹；
  **具体怎么分 AI 不管，做成配置项**让用户自己定义（类目列表 + 归类依据）。
- **压缩包处理（已拍板）**：
  - 解压 → 内容归位 → **原压缩包直接删除**；
  - 解压结果两种情况：
    1. 解压出来是**一个文件夹**（内容在里面）→ 直接用这个文件夹（挂到对应二级分类下）；
    2. 解压出来是**一大堆散文件**（没有文件夹）→ **新建主题文件夹，把散文件移进去**。
- **解压能力现状（待调研）**：App 网页端自身解不了 115 云端的压缩包，需走 **115 自家在线解压接口**（网页版 115 有此功能），
  API 通道与限速要调研验证后才能定实现方案。

## 5. 文本规范（待讨论）

## 6. 音频规范（待讨论）

## 7. 压缩包规范（待讨论）

## 8. 讨论进度跟踪

| 主题 | 状态 |
|---|---|
| 水印清洗内置 | ✅ 已拍板 |
| 文件类型清单 | ✅ 基本定（5+3 类） |
| 电影命名 | ✅ 已拍板 |
| 电影文件夹归置 | ✅ 已拍板（人物/系列/动画/地区；地区五档） |
| 电视剧 | ✅ 已拍板（定名「剧集」；SxxExx；多季且>100集才建季夹；按地区分；纪录片为特殊分类与地区并列） |
| 动漫 | ✅ 已拍板（类剧集；日漫/国漫/欧美/其他；特殊：里番、长漫>200集） |
| AV | ✅ 已拍板（一夹一片；4件套；CD1/CD2；夹名=番号+空格+标题；剧照 标题.fanart.jpg / 海报 标题.poster.jpg） |
| 单视频 | ✅ 已拍板（单视频电影一夹一片；网络杂片并入「归档」统一处理） |
| 小视频 | ⏸ 用户明确「碰到了再说」 |
| 图片 | ✅ 已拍板（三类：漫画/写真/网络杂图；小图清理阈值待定；漫画短/长/合集/PDF + 命名位数；写真五维度+夹名格式+P/V 结构全闭环） |
| 归档（杂图/杂片/压缩包） | ✅ 主体已拍板（顶层「归档」+ 二级分类配置项 + 解压后删原包 + 散文件建夹移入）；解压 API 待调研 |
| 文本 / 音频 | ⬜ 待讲 |
| 转规则落地 | ⬜ 规范全部讨论完后 |

## 9. 悬挂的确认点

1. 人物清单 / 系列清单怎么来：AI+TMDB 自动认，还是用户给手动清单，或混合（知名人物内置、系列查 TMDB）？
2. 补充的「字幕 / NFO / 未知兜底」三类用户未明确表态（默认跟随视频 + 兜底不动）。
3. 小图清理阈值：多少算「太小」（KB 口径）？用户强调要斟酌（AI 建议 <30KB 清、30~100KB 不动，待拍板）。
