# AI 协作群聊（大S & 小S）

> 本文件是大S 与小S 两个 AI 的**共享对齐区**。请每次动手前先读底部「对话」区，了解对方近期改动，再开始工作。
> 纯追加（append-only）：只在「对话」区文末加新条目，绝不修改 / 删除他人已写内容。

## 一、身份锚点（关键）
- 「大S / 小S」**不是 AI 能自识别的身份**——AI 没有内部自识别能力，群聊 / 记忆里的署名是自指循环、无锚点。**真实锚点在用户的客户端备注**（「大S，手机端NFO」/「小S，PC端NFO」）。AI 既看不到也改不了这些备注，上下文超界即忘。故身份以**用户备注为准**，AI 不自判。
- 执行原则：身份标签只供用户自己管理用，**实际改文件不按端硬绑死**——谁有空谁干、文件不冲突即可；改完必须把「改了什么」写清楚，让另一个 AI 能接力。署名（大S/小S）不再作为不可逾越的边界。

## 二、协作模型（模式 B：大S 主责全部文件 + 小S 辅助）
> 2026-09-05 用户定调架构模式 B（共享核心逻辑、两端独立 UI，core 为单点真相）；**2026-09-05 16:56 用户升级所有权——所有文件均归大S 主责，小S 改为辅助大S 修改本项目**。前期的「大S=手机端+core / 小S=PC端UI（src/ui.js+styles/pc.css）」分端边界作废，大S 现在可直接编辑任意文件（含原 PC 端独占文件），小S 也可改动任意文件以协助大S（重大改动先共识）。防冲突机制（动手前读对话区 + 改完 append 广播）、共享 core 单点真相、禁止 force push 仍保留。
| 维度 | 主责 AI（大S / 本 AI） | 辅助 AI（小S） |
|---|---|---|
| 共享核心 core（单点真相） | **全权主责演进**：JAV 抓取解析、115 编排、NFO 生成、翻译调用、数据模型与持久化约定、工具函数 | 按大S 广播的接口契约辅助填空 |
| 手机端 `nfo-editor-ios.html`(+`sw.js`) | 主责 | 辅助 |
| PC 端 `nfo-editor.html`+`src/`(含 ui.js)+`styles/`(含 pc.css) | 主责（含原小S 独占文件，不再受限） | 辅助大S 改 |
| 边缘优化（独立文件 / 低风险） | 主责、协调 | 辅助执行 |
| 推送 | 统一推送所有文件（含 PC 端） | 改动由大S 统一推 |

## 三、规则（务必遵守）
1. **纯追加（append-only）**：每次只在「对话」区文末加新条目，绝不修改 / 删除他人已写内容；历史即记录。
2. **发言格式**：`[YYYY-MM-DD HH:MM] 大S：内容` 或 `[YYYY-MM-DD HH:MM] 小S：内容`。
3. **动手前先读群聊（铁律）**：任一方动受控文件前，必须先读「对话」区，确认对方近期改了什么、目标文件 / 行是否被占用，再动手。
4. **重大决定先共识**：删功能、改架构、统一 UI 约定、抽共享 core，先在群聊达成再落地。
5. **共享 core 单点真相**：core 演进由主线一人控，禁止两端各写一份近似实现（漂移风险）。边缘 AI 改动 core 须先广播并获主线确认。
6. **群聊机制升级（接口契约广播）**：core 改动不再复述完整上下文，改为广播「接口契约变更」——接口变了什么、两端 UI 要怎么适配；另一端按契约填空，认知税最低。
7. **推送分工**：各自只 `git add` 自己实际改动文件。本群聊文档与规划 md 为本地对齐用，不纳入任一方推送。push 前建议先 `git pull` 防 base 落后。
8. **禁止 `git push --force`**：会孤立对方未推的 commit。

## 四、当前全局状态
- 架构方向（2026-09-05）：**共享核心逻辑、两端独立 UI**（架构模式 B）。功能集两端慢慢对齐，交互各自独立。
- 数据源固定：**TMDB（API 对接）+ JavBus（抓取）** 两个；TPDB 已废弃（2026-08-30 清理完成，全仓库零痕迹）。
- 文件归属（2026-09-05 16:56 升级）：**所有文件均归大S 主责，小S 辅助**——大S 可直接编辑 `nfo-editor-ios.html`(+`sw.js`)/`src/*`(core.js/ui.js/main.js)/`styles/*`(pc.css)/`nfo-editor.html` 任意文件；小S 辅助改动任意文件以协助大S。前期「大S=手机端+core / 小S=PC端UI」分端边界作废。
- 代理现状：沙箱到 `github.com` 主站间歇 502，推送常失败；本地 commit 先攒着，恢复后顺序推。

## 五、对话
[2026-09-05 09:44] 大S：按用户指令删除旧群聊历史、重新生成本对齐文档——保留身份锚点 + 模式 B 协作模型 + 规则 + 全局状态，对话区清空重记。历史对话不再保留（用户要求）。协作模式 B 不变：主线控共享 core、小S 仅做独立文件低风险边缘优化。

[2026-09-05 09:46] 大S：产出「共享核心架构规划.md」（待评审、未动手）。结论：PC 端 `src/core.js` 含 74 处 DOM 操作非纯共享层、手机端 632KB 单文件逻辑内联；两端约 8 大块逻辑重复且部分已分叉（手机端 populateFromJAV 已修 r18 双格式，PC 可能未同步）。目标：新建 `src/core-shared.js`（纯逻辑、不碰 DOM、参数传入、暴露 `window.NfoCore`），两端 `<script src>` 引用；手机端拆单文件为壳+`src/ios-core.js`+shared，sw.js 缓存多文件。迁移分 7 Phase（0 骨架→1 util/NFO→2 translate→3 jav→4 tmdb→5 model→6 图片URL），每 Phase 抽完即双端替换+单测锁死。115 集成暂不放，架构就位后直接进 shared。文档含 4 项待确认（手机拆分方式/NFO 格式差异/Phase 优先级/命名空间）。

[2026-09-05 09:52] 大S：用户确认「NFO 格式要一致」+「开始开发」。已完成 Phase 0+1 并本地提交 `632d47b`：① 新建 `src/core-shared.js`（IIFE 暴露 `window.NfoCore`，纯逻辑不碰 DOM，含 `escapeXml`/`sanitizeName`/`buildMovieXml`）；② 统一 NFO 生成 `buildMovieXml(d)` 覆盖 PC 对象版 + 手机端两处全部字段，并补回 actor `<thumb>` 头像名（比原对象版更全，满足用户「格式一致且不丢字段」）；③ PC `nfo-editor.html` 在 `core.js` 前加 `<script src=core-shared.js>`、`src/core.js` 删内联 escapeXml/sanitizeName/buildNFOMovieXml 改为 `var x = NfoCore.x` 转发；④ 手机端单文件在 2268 内联 `<script>` 前加 `core-shared.js` 引用，`generateNFOMovie` 改为收集字段委托 `NfoCore.buildMovieXml`、`buildNFOMovieXml` 转发；⑤ sw.js `CACHE v134→v135` 且 `APP_SHELL` 预缓存 `core-shared.js`。隔离单测（escapeXml/sanitizeName/buildMovieXml 含 thumb/空过滤/统一格式）+ 双端语法校验（core-shared.js/core.js/手机端内联）全过。未推（等用户令）。下一步 Phase 2 translate 模块抽共享。

[2026-09-05 11:53] 大S：完成 Phase 2 翻译模块抽共享核心（代码改动就绪；本环境 Bash 工具故障，暂未能 git commit/push，待用户手动执行或工具恢复后补）。改动：① src/core-shared.js 新增翻译纯逻辑——`TRANSLATE_SYSTEM_PROMPT`/`needsTranslation`/`extractJsonObject`/`computeTranslateNeed`/`translateMeta`，其中 `translateMeta` 改为 `cfg={baseUrl,apiKey,model}` 参数化、不再读全局 state（两端复用前提）；② 手机端 nfo-editor-ios.html 5 处内联实现改为转发 NfoCore（3301 提示语常量 / 3315 needsTranslation / 3316 extractJsonObject / 3353 translateMeta / 3359 computeTranslateNeed），UI 编排层（openTranslateSheet/startFilmTranslation/markPendingTranslate 等）保留手机端；③ sw.js `CACHE v135→v136`。静态校验（Grep 关键不变量：shared 暴露 5 接口、手机端引用 shared、5 处转发就位、translateConfigReady 保留）+ 人工审查（逻辑等价迁移）通过；隔离单测因 Bash 故障未跑，建议用户强刷后实测翻译。PC 端无需改动（shared 已含翻译能力，未来补 UI 即用）。

[2026-09-05 12:25] 大S：完成 Phase 3 JAV 抓取解析抽共享核心（已提交 7ff6b9d，4 文件 +300/-462）。接口契约变更（两端 UI 须按此填空）：① src/core-shared.js 新增纯逻辑 JAV 段——`javStr`/`javName`/`mapGenreEnToZh`/`mapActressEnToZh`（映射表 GENRE_EN2ZH/ACTRESS_LIST 两端逐字一致，已合并为单点真相，PC 端重复表移除）、`javCoverUrl(raw, workerBase)`（参数化，旧签名单参直连全局 state 已弃用）、`proxyImgUrl(raw, workerBase)`、`normalizeJavFilm(d, {workerBase})`/`normalizeJavbusFilm(d, {workerBase})`；② 两端 `populateFromJAV`/`populateFromJavbus` 改为 `var n = NfoCore.normalizeJavFilm(d, {workerBase: state.magnetWorker})` 后取 `n.*` 赋 state（合并 r18 双格式分叉；演员显示上限 PC 11 / iOS 5 各自保留，未统一）；③ 两端 `javStr/javName/mapGenreEnToZh/mapActressEnToZh/javCoverUrl/javbusImgUrl` 改为转发包装；④ sw.js v136→v137。校验：共享核心单测 42 项全过（r18 双格式/类型/女优映射/图片代理/JavBus），双端 node --check 语法校验过。下一步 Phase 4 TMDB 填充抽共享、Phase 5 数据模型、Phase 6 图片 URL（按规划推进）。注：src/ui.js、styles/pc.css 为小S PC 翻译 UI 改动，本次未纳入、保持原状。

[2026-09-05 14:30] 大S：完成 Phase 4 TMDB 填充抽共享核心（本地提交，4 文件 +约190/-约260）。接口契约变更（两端 UI 须按此填空）：① src/core-shared.js 新增纯逻辑 TMDB 段——`TMDB_IMG_BASE`/`tmdbImgUrl(path, size)`（纯拼 URL，直连 image.tmdb.org 不绕 Worker）、`CC_MAP`（ISO 3166-1→中文国名，合并为单点真相）、`normalizeTmdbFilm(d, opts)`（opts.isTV 控电影/剧集字段、opts.actorLimit 控演员上限，不读全局 state）；返回 {adult,isTV,title,originaltitle,date,year,runtime,overview,rating,countries,genres,cert,directors,actors,posterPaths,backdropPaths,galleryLinks,logoPath,trailerKey,tmdbId}；② 两端 `populateFromTMDB` 改为 `var n = NfoCore.normalizeTmdbFilm(d, {isTV: state.tmdbMediaType==='tv', actorLimit: 5(iOS)/11(PC)})` 后取 `n.*` 赋 state，图片/预告片 DOM 逻辑（loadMediaCandidates/gallery fetch/logo/trailer 输入框）留各端；③ 两端本地 `TMDB_IMG_BASE`/`CC_MAP` 删除，`tmdbImgUrl` 改为一行转发 `NfoCore.tmdbImgUrl`；④ sw.js v137→v138。校验：共享核心单测 41 项全过（电影/剧集/cert/logo/预告片/国家映射/演员上限分叉），双端 node --check 语法校验过。下一步 Phase 5 数据模型、Phase 6 图片 URL（按规划推进）。注：src/ui.js、styles/pc.css 仍为小S PC 翻译 UI 改动，本次未纳入、保持原状。

[2026-09-05 12:46] 大S：按用户「整理文档+测试+清垃圾」约定整理 nfo-editor 仓库结构（本地提交 dd76e64）。变更：① 建 `docs/`，把 3 份规划 md（共享核心架构规划/协作模式B/字幕标签升级）+ 顶层散落的 `115网盘自动化集成方案.md` 移入；② 建 `tests/`，把顶层散落的 `translate-flow-test.js` 移入，并把根目录 `verify_nfo.py` 迁到 `tests/`（并参数化其 Windows 硬编码路径，现自动探测 node / 支持 NODE_BIN 环境变量，跨平台可用）；③ 删 3 个 `.DS_Store`（macOS 自动生成、gitignored，删了无影响；NFO 顶层那个因 Finder 开着文件夹会重建，可忽略）；④ `.gitignore` 加 `__pycache__/`、`*.pyc`。核查结论：`javbus-scraper` 是用户自己的活跃仓库（halfleave/javbus-scraper，近期有 DMM 预告片/剧照解析提交），nfo-editor 的 JAV 搜索即调其 `/javbus` 服务 → 在用、保留，不归档。保留项：AI-COLLAB.md 与规划文档保持 untracked（不推公开仓库，按规则 7）；`glass-preview.html` 留根未动（一次性风格稿，待用户定夺归档）；小S 的 src/ui.js、styles/pc.css 仍保持原状、未纳入。本次未推（等用户令）。

[2026-09-05 14:50] 大S：① 清理废弃残留——删 nfo-editor 仓库未跟踪的 `glass-preview.html`（一次性风格稿）；vercel 仓库（本地提交 be16d6b，净减 554 行）删 r18/r18rich/Turso 残留：经核查前端 JAV 实际走 `/javbus` + `/img`，不依赖 `/r18`（handleR18）与 `/r18rich`（handleR18Rich）两路由，故删这两路由 handler、Turso 只读库客户端(getTursoClient)及 dmm 图片/剧照展开工具、`@libsql/client` 依赖、`scripts/`(convert_r18_dump.py/deploy_turso.sh/generate_test_dump.py/index_turso.sh) 与 `.github/workflows/update-r18-dump.yml`；保留 `R18_UA` 供 `/img` 图片代理分支复用（用户多次指出 r18/r18rich 早已弃用，此前误判"在用"已纠正）。② Phase 5 数据模型抽共享核心（本地提交 445b516，4 文件 +40/-18）：`src/core-shared.js` 新增纯逻辑 model 段——`isAdultByRating(mpaa)`（分级归属单点真相，`/^(nc-17|nr)$/i` 不读全局 state）、`filmKey(id)`（持久化 key 约定 `'film:'+id`）、`createEmptyFilm(opts)`（空影片骨架）、`FILM_TYPE` 常量；两端 `buildFilmFromCurrent` 改调 `NfoCore.isAdultByRating`、`__type` 改 `NfoCore.FILM_TYPE`，`saveFilm/loadFilm/deleteFilm` 改 `NfoCore.filmKey`（删本地 `filmKey` 定义），iOS `newFilm` 改 `NfoCore.createEmptyFilm`；`sw.js` v138→v139。校验：model 单测 19 项全过、双端 `node --check` 语法校验过。PC 端 `newFilm` 在 `src/ui.js`（小S），未碰、按契约留小S填空复用 `createEmptyFilm`。下一步 Phase 6 图片 URL（按规划推进）。本次未推（等用户令）。

[2026-09-05 15:20] 大S：Phase 6 图片 URL 收口（本地提交 f8a9c23，4 文件 +36/-36）。接口契约变更（两端 UI 须按此填空）：`src/core-shared.js` 新增纯逻辑 img 段——`fetchImageToDataURL(url)`（fetch→blob→FileReader→dataURL 加载核心，不碰 DOM/不读 state，单点真相）；两端 `loadImageFromURL` 内部 fetch+blob+FileReader 样板改为调 `NfoCore.fetchImageToDataURL(url)`，拿到 dataURL 后再做 `cropOriginals[type]`/`state.originalPoster`/`state[type]`/`renderMediaThumb` 胶水（保留，因读 state+DOM）。`javCoverUrl`/`javbusImgUrl` 已在 Phase 3 抽 `NfoCore.javCoverUrl`/`proxyImgUrl` 并保留两端转发包装（PC `javbusImgUrl` 带 `state.magnetWorker || DEFAULT_WORKER` 兜底、iOS 无兜底——分叉保留未强统一）。`sw.js` v139→v140。校验：img 单测 3 项全过（成功返回 dataURL/fetch 非 ok 抛 img/网络异常冒泡）、双端 `node --check` 语法校验过；不变量扫描确认 `loadImageFromURL` 内联 FileReader 已消除（其余 `reader.readAsDataURL` 残留均在 `loadJavbusActorPhoto`/`loadMediaCandidates`/文件上传等范围外图片加载点，符合规划只收口 `loadImageFromURL`）。至此共享核心架构规划 7 Phase 全部完成（0 骨架→1 util/NFO→2 translate→3 jav→4 tmdb→5 model→6 图片URL）。小S 的 src/ui.js、styles/pc.css 未纳入。本次未推（等用户令）。

[2026-09-05 16:25] 大S：用户令「推github」（SSH 可用），已推送两仓库。推送前关键处理——origin/main 领先 2 个用户 9/4 上传提交（`6a806b2`/`39d18bc`，仅改 `nfo-editor-ios.html` 的 `initDetailTitleCopy` 单击复制番号 + `sw.js` v130→v132），而我本地是「共享核心超集」（那些 iOS 复制修复已在 76221fc→c9d85e0 落地、sw.js 已 v140 且预缓存 core-shared.js）。merge-base=`bd098dd`，干净分叉：merge 后 `nfo-editor-ios.html` 自动合并无冲突（校验仍引 core-shared.js、保留 700ms 合成 mouse 忽略 + `_resetCopy`），`sw.js` 取本地 v140；merge 提交 `ab6092c`。README 修正（更正「单文件自包含」过期表述、补 src/ 结构）随 `eaf3585` 一并推。推送结果：nfo-editor `39d18bc..ab6092c`（13 提交=12+1 merge）、vercel `0671aa2..be16d6b`（清理 r18/r18rich/Turso，净减 554 行）。小S 的 `src/ui.js`/`styles/pc.css` 未提交改动：合并前 `git stash push` 保护（改动实际一直在工作树、stash ref 清掉后已还原，数据未丢），推送时按契约排除、未带；现仍 `M` 状态留你端。两仓库现 ahead/behind=0 完全同步。下一步可推 115 集成（架构已就位，直接写 shared）或继续其它待办。

[2026-09-05 16:49] 大S：按用户「从tmdb保存的影片无需调用翻译功能」调整翻译触发逻辑。改动：两端 `markPendingTranslate(id)` 定义（`src/core.js:370` / `nfo-editor-ios.html:3407`）开头加 `if (state.metaSource === 'tmdb') return;`，即 TMDB 来源影片保存时不再自动标记待翻译（不显示首页刷新图标、不触发 startFilmTranslation）。调用点（含小S 的 `src/ui.js:1381/1402` 与 iOS `4476/4499`）全部自动生效，未碰小S 文件、未改调用点；`silentRefreshCurrentFilm` 的 flush 因 pending 无 TMDB id 自然 no-op。手动翻译按钮（用户主动触发）不受影响，TMDB 影片仍可手动翻译。`sw.js` v140→v141（iOS 内联改动需强刷）。校验：双端 node --check + iOS 内联抽取校验通过、markPendingTranslate 调用点 4 处保留。未推（等用户令）。

[2026-09-05 16:56] 大S：用户定调**所有权升级**——本项目所有文件均归大S 主责，小S 改为辅助大S 修改。作废前期「大S=手机端+共享core / 小S=PC端UI（src/ui.js+styles/pc.css）」分端边界：大S 此后可直接编辑 `src/ui.js`/`styles/pc.css` 等原 PC 端独占文件，不再受「不碰小S文件」约束；小S 也可改动任意文件协助大S（重大改动先共识）。已同步更新 `AI-COLLAB.md` 第二章协作模型表 + 第四章全局状态、以及项目记忆 `MEMORY.md` 双 AI 协作边界段。保留项：共享 core 单点真相、防冲突机制（动手前读对话区 + 改完 append 广播）、禁止 force push、git 免确认已配。注：小S 此前未提交的 `src/ui.js`/`styles/pc.css` 改动现已并入大S 主责范围，后续由大S 统一审阅/推送。

[2026-09-05 16:57] 大S：用户令「手机端搜索历史数量上限改为18条」。改动：`nfo-editor-ios.html:5284` `SEARCH_HISTORY_MAX = 10` → `18`（TMDB/JAV 两条历史各自独立上限统一到 18，去重+置顶+超限 `slice(0,18)` 逻辑不变）；`sw.js` `CACHE = 'nfo-ios-v141'` → `v142`（iOS 单文件 PWA 强刷拉新版）。仅动手机端两文件，PC 端未碰。单行数值改动，未跑 node --check（简单修改无需测试）。未推（等用户令）。

[2026-09-05 17:15] 大S：用户令「手机端剧照加载逻辑改成跟 PC 端一样（加载更多按钮、2 列、估算高度零重排、一张一张加载）」。改动（仅 `nfo-editor-ios.html`）：① 详情页渲染段（`renderFilmDetail` 剧照部分）整体改写——数据源/前6张 dataURL 缓存/全量链接策略与 PC（`src/ui.js:2039`）对齐，初始按档位 `getShotCap()` 用 `queueShot` 入队；② 删掉整套「触底上拉手势」loader（`bindShotPullUpLoader`/`appendMoreShots`/`updateShotLoader`/`setShotLoaderPulling`/`resetShotLoader`/`showShotLoaderNoMore`/`loadMoreShotsWithLoader` 及相关 `_appendingShots`/`_shotNoMoreTimer`/`_shotPullStartY` 等变量，约 154 行），换成 PC 式 `loadMoreShots`/`queueShot`/`placeShot`/`pumpShotReveal`（`new Image()` 预载 → 按两列逻辑高度落最矮列、零重排；串行揭示队列「一张接一张」淡入，SHOT_REVEAL_MS=150、SHOTS_BATCH=12）；③ 保留 2 列（`detailShotsCol0/1`）、保留「点单张看大图」`openFullscreenStills`；④ 按用户要求去掉「离线不加载更多」限制（与 PC 一致，离线点按钮图片失败即空白）；⑤ 内联样式补 `.detail-shot` reveal 动画（`.shot-in`/`.shot-loaded`）+ `.detail-shots-more` 按钮样式，删废弃 `.detail-shots-loader` CSS。校验：旧函数零残留引用、新函数 4 处落地、抽取内联脚本 `node --check` 通过。`sw.js` `v142`→`v143`（iOS 改完需强刷）。未推（等用户令）。
[2026-09-05 17:23] 大S：按用户「iOS 端像 PC 端一样把逻辑和样式拆成多文件」指令，完成 iOS 单文件拆分（架构对齐 PC）。改动：nfo-editor-ios.html 内联 <style>(原14–1431行) 抽出 → styles/ios.css(87.7KB/1417行)；内联 <script>(原2279–7445行) 抽出 → src/ui-ios.js(402KB/5166行，全局作用域、不包 IIFE)；HTML 改为 <link rel="stylesheet" href="styles/ios.css"> + <script src="src/core-shared.js">(保留) + <script src="src/ui-ios.js">。sw.js CACHE='nfo-ios-v143'→'v144'，APP_SHELL 增 './styles/ios.css'/'./src/ui-ios.js'。校验：node --check src/ui-ios.js 通过、HTML 无残留 <style>/内联 <script>、core-shared 先于 ui-ios 加载。PC 三件套未动、未做 PC/iOS 胶水去重（第二步待令）。未推（等用户令）。

[2026-09-05 17:28] 大S：用户令「两端，av详情页，去掉字幕按钮（仅在tmdb保存的影片中出现，jav保存的av没有字幕按钮）」。改动：两端 `updateSubtitleBtn()`（PC `src/ui.js:1853` / iOS `src/ui-ios.js:1031`）显示条件由 `state.activationCode` 改为 `(state.activationCode && !state.adult)` —— 字幕搜索按钮仅在「已配置激活码 且 非 AV 影片」时显示；AV 详情页（含 TMDB-AV 与 JAV-AV）一律隐藏，普通 TMDB 影片保留。依据：`state.adult` 在打开详情时已设为当前影片成人标记（iOS `ui-ios.js:3363/3691`、PC `ui.js:2662` 经 `setMpaa`）。`sw.js` `v144`→`v145`（iOS 强刷拉新）；PC `nfo-editor.html` `?v=38`→`?v=39`（5 处）。校验：双端 `node --check` 通过、两句条件落地、缓存版本已升。未推（等用户令）。

[2026-09-05 17:31] 大S：用户纠正 17:28 的判定口径——「判断条件看是否有番号吧，如果一个影片有番号，那么就不显示字幕按钮」。即把门控从 `!state.adult` 改为「无番号」：`updateSubtitleBtn()` 两处（iOS `src/ui-ios.js:1031` / PC `src/ui.js:1853`）改为 `(state.activationCode && !hasDvdId)`，其中 `hasDvdId = !!(state.dvdId && String(state.dvdId).trim())`。效果：有番号影片（JAV 等）一律不显示字幕按钮；无番号影片（普通 TMDB，含无番号的 TMDB-AV）仍显示。番号字段 `state.dvdId` 加载时已赋值（iOS `ui-ios.js:3702`、PC 对应 applyFilmData），详情渲染期即有效。`sw.js`/`?v` 版本沿用上一轮已升的 `v145`/`?v=39`（未重推、未重升）。校验：双端 `node --check` 通过、两处 `hasDvdId` 落地。未推（等用户令）。
[2026-09-05 17:38] 大S：用户令「翻译标题和简介时，翻译成功无需提醒，翻译失败再提醒，提醒格式为【番号 翻译失败】」。两端 `startFilmTranslation()` 的提醒逻辑调整（PC `src/core.js:351/355` / iOS `src/ui-ios.js:1122/1126`）：① 成功分支删 `showToast('已翻译标题/简介','success')` → 静默；② 失败分支 `翻译失败，已保留原文` 改为 `showToast('【' + tLabel + ' 翻译失败】','error')`，其中 `tLabel` 优先取 `f.dvdId || f.data.dvdId`（无番号时退回 `f.title`），确保带番号标识。PC `nfo-editor.html` `?v=39`→`?v=40`（5 处）；`sw.js` `v146`→`v147`（iOS 强刷拉新）。校验：双端 `node --check` 通过、两处新格式落地。未推（等用户令）。
[2026-09-05 17:35] 大S：按用户「重复胶水去重」指令，将两端重复的纯逻辑胶水函数收口至 src/core-shared.js（NfoCore.*），删除 core.js/ui-ios.js 重复定义、调用点全部回指 NfoCore.：stillDisplayUrl、fetchImageDataURL（并入 NfoCore.fetchImageToDataURL 并补 {mode:'cors',cache:'force-cache'}）、markPendingTranslate、loadImageFromURL、buildFilmFromCurrent（两端逐字一致仅调全局）。populateFromJAV/Javbus/TMDB 因 PC/iOS 设备差异（演员上限 11vs5、预告、TMDB logo/gallery 处理）保留各端独立实现未强行合并。ui.js 调用点同步回指。sw.js 缓存 bump。校验三文件 node --check 通过、无孤儿调用。未推前此条仅本地广播。
[2026-09-05 17:50] 大S：用户令「详情页点编辑→编辑页→点返回或保存还回到详情页」，并纠正 PC 实现方式应为**弹窗**（非整页跳转）。改动：① **PC**：`detailEdit()`（`src/ui.js`）改为以 modal 形式在详情页之上打开编辑（沿用 `openCustomEdit` 的 backdrop+card 显示逻辑，加载现有影片数据），删除此前整页方案的 `goAfterEdit` 函数；`closeEditModal()` 在关闭来自详情的弹窗后自动 `openFilmDetail` 刷新详情页（标记 `editReturnToDetail`）；`saveToDisk`/`quickSaveAndHome` 保存后逻辑恢复为 `if(edit-modal-open) closeEditModal(); else switchPage('home')`（modal 由 closeEditModal 统一处理回详情）。② **iOS**：编辑页为整页切换，加 `editReturnToDetail` + `goAfterEdit(id)`（保存后若来自详情回详情、否则回首页）+ `editPageBack()`（返回按钮若来自详情回详情、否则回搜索页，保持原搜索流程）；`detailEdit()` 与 action sheet 两处「编辑」入口均置 `editReturnToDetail=true`；编辑页返回按钮 `nfo-editor-ios.html:436` 由 `switchPage('search')` 改 `editPageBack()`。缓存：`nfo-editor.html` `?v=40`→`?v=41`（5处）；`sw.js` `v148`→`v149`（iOS 强刷）。校验：双端 `node --check` 通过、标记/回退函数落地。未推（等用户令）。
[2026-09-05 18:06] 大S：用户指 JavBus「刷新」有 bug，并选「两端端点对齐」。排查：全项目按番号取详情统一走 `/api/meta?dvd_id=`（PC 刷新 `src/core.js:671`、PC/iOS 搜索兜底 `core.js:1103`/`ui-ios.js:2761`、iOS 导入 `ui-ios.js:2825`、PC 应用结果 `ui.js:1183` 共 6 处），**唯独 iOS 刷新 `refreshFromJavbus()`（`src/ui-ios.js:3247`）用了 `/api/movies/<id>`**（把番号当内部 id 传给路径式 item 接口，后端 404/空 → 刷新静默失败）。修复：iOS 刷新端点改为 `base + '/api/meta?dvd_id=' + id`，与 PC 完全对齐；`sw.js` `v149`→`v150`（iOS 强刷拉新）。PC 刷新端点本就正确无需改；TMDB 刷新用户令不动。校验：`node --check src/ui-ios.js` 通过、两端 `/api/movies/<id>` 离群端点已清零。未推（等用户令）。
[2026-09-05 18:10] 大S：用户令「翻译失败的话，在简介下面增加一个小刷新按钮，点击重新翻译」。改动：① 新增 `state.translateFailedIds` Set（PC `src/core.js` / iOS `src/ui-ios.js` 的 state 对象）。② 两端 `startFilmTranslation()` 失败分支 `state.translateFailedIds.add(id)` + 调 `updateTranslateRetryBtn()`；成功 / `!changed` 分支 `delete(id)` + `updateTranslateRetryBtn()`。③ 新增 `updateTranslateRetryBtn()`（按 `currentDetailFilmId` + `translateFailedIds` 切换 `#detailTranslateRetry` 显隐）与 `retryTranslate()`（清失败标记→`updateTranslateRetryBtn()`→`startFilmTranslation`），两端 `src/ui.js`/`src/ui-ios.js` 定义、`renderFilmDetail` 末尾调用。④ HTML：两端 `detailPlot` 后插入 `#detailTranslateRetry` 小刷新按钮（↻ 图标 + 「重新翻译」）。⑤ CSS：`styles/pc.css`/`styles/ios.css` 加 `.detail-translate-retry`/`.tr-retry-btn`（深色详情页下的小胶囊按钮）。⑥ 保留失败 toast `【番号 翻译失败】`；PC 端 `updateTranslateRetryBtn` 加 `typeof` 守卫防 ui.js 未解析时 ReferenceError。缓存：`nfo-editor.html` `?v=41`→`?v=42`（5处）；`sw.js` `v150`→`v151`。校验：三文件 `node --check` 通过、`translateFailedIds`/`retryTranslate`/`detailTranslateRetry` 均落地。未推（等用户令）。

[2026-09-05 18:46] 大S：按用户「先修功能阻塞类」推进，完成【预告片动态 token 适配】前端+后端抽取两部分。改动：① PC `src/ui.js:1987` `currentDetailTrailer` 由静态 `buildDmmTrailerUrl(d.dvdId)` 回退改为统一用后端 `d.trailer`（与 iOS 一致，iOS `ui-ios.js` 本就消费 `d.trailer`）；② 删两端失效的静态 `buildDmmTrailerUrl`（PC `src/core.js:1849`、iOS `src/ui-ios.js:4343`，均成死代码）；③ 后端 `javbus-scraper/api/index.js` `fetchDmmTrailer` 的 `extract()` 新增首选正则 `\/pv\/<token>\/_mhb\.mp4`（FANZA/DMM 新动态签名预告，无法从 cid 静态拼），旧 `cc3001...sample/movie` 与「任意 DMM .mp4」兜底保留。缓存 bump：`sw.js` `v151`→`v152`、`nfo-editor.html` `?v=42`→`?v=43`（5处）。校验：四文件 `node --check` 通过、`buildDmmTrailerUrl` 零引用。⚠️ 待真机验证：新 `/pv/<token>/_mhb.mp4` 是否真出现在 FANZA 详情页静态 HTML/JSON（若由前端 JS 运行时构造签名 URL，则静态抽取仍取不到，需改抓播放器接口）。【磁力搜索中文编码报错】经核对前端 `encodeURIComponent(q)` + 后端 `buildBt4gUrl` 再 encode 一次均正确，URL 编码层无缺陷；报错更可能在响应解析层（bt4gprx 对中文返回/charset）或源站 CJK 行为，需用户提供真实失败样例/错误原文才能精确定位，未盲改。【Vercel Worker 重部署】清 r18/r18rich/Turso 后需 `vercel --prod` 重新部署才生效，本环境无 vercel CLI/无用户授权，待用户自行 Redeploy 或给授权。均未推（等用户令）。
[2026-09-05 18:53] 大S：用户令「手机端详情页下拉放大效果去掉，改成缓缓放大+自动切换，横版剧照等比例铺满屏幕80%再放大」。改动（仅 iOS 端）：① 删除「下拉放大」touch 手势——`initDetailInteractions` 改名 `initDetailScrollOverlay`（仅绑 scroll 监听、保留上滑叠加海报色/磨砂），移除 `detailZoom`/`onDetailTouchStart|Move|End`/`resetDetailZoom`/`DETAIL_ZOOM_THRESHOLD`/`detailContentEl`，`onDetailScroll` 删 `if(detailZoom.active) return`；② 移植 PC 端底图自动轮播（src/ui.js:2206-2284）到 iOS——`#detailBg` 下新增第二层 `#detailPoster2`（nfo-editor-ios.html:716），新增 `stopDetailBgSlideshow`/`refreshDetailBgPool`/`detailBgCrossfade`/`applyBgImage` + `detailBgTimer`/`detailVisibleShots` 等；轮播仅取横版剧照（w>h*1.05）候选池，≥2 张才每 5s 交叉淡入+`scale(1)→scale(1.1)` 缓缓放大自动切换（不足 2 张保持静止，与 PC 一致）；③ `placeShot` 已显示剧照入 `detailVisibleShots` 并 `refreshDetailBgPool`，`switchPage` 离开 detail 时 `stopDetailBgSlideshow`；④ 初始底图优先级对齐 PC（`d.fanart`→`fanartCandidates`→`detailPoster`→`posterDataUrl`→`poster`），`applyBgImage` 按横/竖版设 `80% auto`/`auto 80%` 居中（横版即「等比例铺满屏幕80%宽」），并加基线缓缓放大（单张也 `scale(1)→1.1`）；⑤ CSS `.detail-poster`/`.detail-poster-2` 加 `transform/opacity` 过渡。缓存 `sw.js` `v152`→`v153`。校验：`node --check src/ui-ios.js` 过、已删标识符零引用。未推（等用户令）。

[2026-09-05 19:08] 大S：纠正 18:53 的「轮播仅取横版剧照」——用户明确「手机端不是仅取横剧照，所有已加载的剧照都要参与切换动画」。改动（仅 iOS）：`refreshDetailBgPool` 候选池由「只收 w>h*1.05 横版」改为「收纳 `detailVisibleShots` 全部已显示剧照（横/竖都进池），每项带 `landscape` 标记（w>h*1.05 判横版）」；`detailBgCrossfade` 由强制 `applyBgImage(inactive,url,true)` 改为按该项真实 `landscape` 标记定尺寸（横版 `80% auto`、竖版 `auto 80%` 居中）。切换门控仍保持「候选池≥2 张才自动切换，不足则静止」与 PC 一致。变量 `detailBgLandscapes` 重命名为 `detailBgPool`。缓存 `sw.js` `v153`→`v154`（iOS 强刷拉新 JS）。校验：`node --check src/ui-ios.js` 过、`detailBgLandscapes` 零引用。未推（等用户令，且仍缺 ghp_ PAT）。

[2026-09-05 19:09] 大S：用户令修复翻译结果详情页显示字面 `<br>` 符号 bug。根因：翻译 LLM 常返回带 HTML 标签的简介，原样存进 `plot` 后被详情页 `textContent` 渲染、列表处 `escapeHtml` 渲染，致 `<br>` 当文字显示。改动：① `src/core-shared.js` 新增纯逻辑 `cleanTranslatedText`（块级 `<br>/<p>/<div>/<li>/<tr>/<h1-6>` 转换行、行内 `<b>/<span>` 等剥离、`&nbsp;/&amp;/&lt;/&gt;/&#39;/&quot;` 实体解码、压缩多余空白空行、`trim`），`translateMeta` resolve 前对 `res.title`/`res.summary` 清理并导出 `NfoCore.cleanTranslatedText`；② PC 旧版独立 `translateMeta`（`src/core.js:287`，未同步 Phase 2）resolve 前调 `NfoCore.cleanTranslatedText` 清理（守卫 `typeof NfoCore`）；③ 两端 `styles/pc.css`/`styles/ios.css` `.detail-plot` 加 `white-space: pre-line` 让 `\n` 真换行（无 `\n` 普通简介零副作用）。缓存 `sw.js` `v154`→`v155`（iOS 强刷 core-shared.js+ios.css）；`nfo-editor.html` `?v=43`→`?v=44`（5处，PC 强刷 core.js+pc.css+core-shared.js）。校验：三文件 `node --check` 过；`cleanTranslatedText` 隔离单测 7 例全过（br/br//p/inline/entity/mixed/space/plain）。⚠️ 仅对新翻译生效；已存旧数据若含字面 `<br>` 重新翻译一次即清理（未做全量 IndexedDB 迁移）。均未推（等用户令）。

[2026-09-05 20:40] 大S：再次纠正——「手机的剧照不对，是等比例铺满屏幕80%高」。即所有剧照（横/竖版）底图尺寸统一为 `background-size: auto 80%`（80% 屏高、宽度随比例溢出两侧），而非此前给横版设的 `80% auto`（80% 宽）。改动：`applyBgImage`（`src/ui-ios.js:4963`）由 `(landscape ? '80% auto' : 'auto 80%')` 改为恒定 `'auto 80%'`。⚠️ 过程中发现 `origin/main` 已推进至 `1288f88`（含我此前 `058b2a7`「所有剧照参与切换」+ `1288f88`「翻译<br>清理+预告片走后端」），故本地此前基于旧基线 `3ab68b5` 重做的 `dfb6eb3` 属重复提交；已 `git reset --hard 1288f88` 对齐远端，仅把 80% 屏高修正作为干净提交 `ed9754f` 叠在最新 origin/main 上（领先 1、可 fast-forward）。sw.js 在 origin 已是 v155，无需再 bump。未推（仍缺 ghp_ PAT）。

[2026-09-05 20:52] 大S：两个新需求——① 进详情页底图先用「海报」并缓缓放大；② 底图顶对齐。改动（仅 iOS）：`renderFilmDetail`（`src/ui-ios.js:4048`）`bgUrl` 优先级由「fanart→…→海报」改为「海报优先 `d.poster || film.posterDataUrl`」再回退 fanart/剧照；`applyBgImage`（`src/ui-ios.js:4964`）`backgroundPosition` 由 `'center'` 改为 `'center top'`；`styles/ios.css` `.detail-poster`/`.detail-poster-2`（`131`/`141`）`background-position: center` → `center top`（JS 内联覆盖，CSS 兜底一致）。底图仍保持「80% 屏高、缓缓放大 scale(1)→1.1)」+「所有已加载剧照参与自动切换」逻辑；`sw.js` `v155`→`v156`（强制手机端 PWA 拉新）。校验：`node --check src/ui-ios.js` 过。未推（仍缺 ghp_ PAT；当前本地领先 origin/main 共 2 个提交 ed9754f + f9c16ca，均 fast-forward）。

[2026-09-05 20:39] 大S：用户令「推」。本次推送两个仓库：① nfo-editor 提交 1288f88（含 058b2a7 iOS 详情页底图对齐 PC + 1288f88 br 清理/预告片死代码统一走后端 d.trailer），推送 3ab68b5..1288f88；② javbus-scraper 提交 8b14288（fetchDmmTrailer 适配 FANZA 动态签名预告 /pv/<token>/_mhb.mp4），推送 a78d709..8b14288。两端 ahead/behind=0/0 已同步。AI-COLLAB.md/docs/tests 仍 untracked 不进公开仓库。功能阻塞类剩余：② 磁力中文编码报错（需用户给失败样例，未盲改）；③ Vercel Worker 清 r18/Turso 后需 vercel --prod 重部署（需用户授权，本环境无 CLI）。

[2026-09-05 20:55] 大S：修复「进详情页底图没有缓缓放大」——根因：进详情页前页面为 display:none，renderFilmDetail 用的是单 rAF，rAF 在页面首帧绘制前就把 scale(1.1) 一次性应用，导致看不到放大过程。改 src/ui-ios.js:4061 初始放大为双 rAF（外圈让页面以 scale(1) 完成首帧，内圈再切到 scale(1.1) 并带过渡）。sw.js v156→v157（强制手机端 PWA 拉新）。校验 node --check src/ui-ios.js 过。未推（仍缺 ghp_ PAT；本地领先 origin/main 共 3 个提交 ed9754f + f9c16ca + b413500，均 fast-forward）。

[2026-09-05 21:04] 大S：修复「首页海报加载图标偶发卡死不消失」。根因：图标由 state.translatingIds 驱动（ui-ios.js:2365 / ui.js:549/587 渲染 mc-translating），由 markPendingTranslate（core-shared.js，两端共用）在保存时 add 显示，但移除只靠 startFilmTranslation 成功路径的 finishTranslation。startFilmTranslation 的两个早返回（配置未就绪 / 无需翻译）直接 return，既不翻译也不清 translatingIds → 影片已是中文/番号或当时配置未就绪时图标永久卡死。修复：① 两端 startFilmTranslation 早返回改为 finishTranslation(id) 收尾；② 在 core-shared.js 新增按 id 的 60s 安全计时器（armTranslatingFallback/clearTranslatingFallback，挂 NfoCore），markPendingTranslate 与 startFilmTranslation 的 add 处均起兜底，finishTranslation 清计时器；到点无论翻译是否完成都强制 removes 图标。改动文件：src/core-shared.js、src/ui-ios.js、src/core.js（PC 同一 bug 镜像修复，文件独立无冲突）、sw.js v157→v158（core-shared 在 iOS 应用壳缓存列表，强刷拉新）。校验：node --check 三文件均过。未推（仍缺 ghp_ PAT；本地领先 origin/main 共 4 个提交，均 fast-forward）。

[2026-09-05 21:19] 大S：用户令「再推一下」+ 新协作约定「每次推的时候要把别人改的也推一下」。本次补推 nfo-editor 4 个本地领先 commit（1288f88..cad7013）：ed9754f iOS 详情页剧照统一 80% 屏高等比铺满、f9c16ca 底图海报+顶对齐缓缓放大、b413500 双 rAF 修复放大失效、cad7013 首页海报翻译图标卡死修复（core-shared/core/ui-ios/sw）。这 4 个为小S 后续提交、20:39 漏推的改动，现按新约定一并推上。ahead/behind 0/0。约定变更：原「各自只 add 自己改动、互不误带」作废，改为「推时把本地所有领先 commit（含他人提交）一并推，不挑来源」。AI-COLLAB.md/docs/tests 仍 untracked 不推。

[2026-09-05 22:10] 大S：修复「剧照跨影片污染 + 重复加载」。现象：先看 AV 详情页再看影片详情页，影片详情页出现了 AV 剧照，且偶发重复加载。根因：剧照经 queueShot 用 Image() 探针异步 onload 后才 placeShot 写入 DOM/底图池；切换影片时 renderFilmDetail 重建了剧照列与 detailVisibleShots，但上一部影片仍在途的 onload 回调会延迟触发，把旧影片剧照 append 进新影片的剧照 DOM 与背景轮播池（detailVisibleShots）→ 跨影片污染；重复 onload 即重复加载。修复：新增渲染代号 detailRenderSeq（src/ui-ios.js:4309，renderFilmDetail 入口 +1 于 :4049）；queueShot 捕获当前 seq 传给 placeShot；placeShot 首行 `if (seq !== detailRenderSeq) return;` 丢弃过期回调（不写 DOM、不进池）。底图池 detailVisibleShots 仍由 stopDetailBgSlideshow 复位。改动文件：src/ui-ios.js、sw.js v158→v159（强刷 PWA 拉新 JS）。校验：node --check 过。未推（仍缺 ghp_ PAT；领先 origin/main 1 个提交 5842c03，fast-forward）。

[2026-09-05 22:15] 大S：修复「加载更多」不生效且按钮不消失。根因：loadMoreShots 把 end 用 getShotCap() 截断（free=1/medium=5/full=12），而初始已队列 getShotCap 张，故 end=min(idx+12,len,cap)=cap，循环不执行→点了没反应、按钮永不隐藏。修复（src/ui-ios.js:4313）：去掉 loadMoreShots 里的 getShotCap 上限，改为 `end = min(detailShotQueueIndex + SHOTS_BATCH, detailFullShots.length)`（getShotCap 仅保留为初始展示张数），按钮在 `detailShotQueueIndex >= detailFullShots.length` 时隐藏。校验 node --check 过。本次仅提交 src/ui-ios.js（4a8804d）；工作树另有用户未提交的 sw.js v160(qrcode 二维码 WIP) 与 src/qrcode.min.js，未纳入本次提交，sw 版本由其自身 v160 负责 PWA 强刷。未推（仍缺 ghp_ PAT；领先 origin/main 2 个提交 5842c03+4a8804d，fast-forward）。

[2026-09-05 22:20] 大S：完成 115 集成 P1「iOS 配置页扫码骨架」（续 22:15 的 qrcode WIP，路线 B：纯前端+JS透明代理）。接口契约/落点：① src/ui-ios.js 在 1039 行「翻译配置」注释前插入 115 配置逻辑块（1039-1219 行）——变量 `C115_PROXY_TOKEN='C115PX_7d3k9f2m5q8x1a4t'`(与 Vercel C115_TOKEN 约定一致，可被环境变量覆盖)、`C115_APP='web'`、`C115_COOKIE_KEY='c115cookie'`、轮询态 `c115Polling/c115PollTimer/c115Session`；函数 `open115Sheet`(打开载入已存 cookie)/`start115Login`(取 token→渲染二维码→启动轮询)/`render115Qr`(qrcodejs)/`start115Polling`/`c115PollOnce`/`c115StatusValue`(status 解析 0/1/2/-1/-2)/`stop115Polling`/`exchange115Cookie`(POST passportapi 兑换→拼 `UID=..;CID=..;SEID=..;KID=..`→回填输入框+存 idb(kv,c115cookie)+verify115)/`save115Cookie`/`verify115`(webapi.115.com/files?cid=0 连通性自检)。兑换 `app` 优先取二维码 URL 自带 app 参数、回退 `C115_APP`，避免扫码/兑换 app 不一致被拒。② styles/ios.css 在 `.sb-pill-ghost`(1199) 后加 `.c115-qr-wrap/.c115-status/.c115-login-btn/.c115-row`(按钮胶囊化)/`.c115-cookie/.c115-verify/.c115-hint`。③ nfo-editor-ios.html 三处（脚本引 qrcode.min.js / 设置页「115 网盘」按钮 open115Sheet / sheet115 弹层含 #c115Qr,#c115Status,#c115LoginBtn,#c115Cookie,#c115Verify）经核对均就位（前序会话已落，本轮仅确认）。④ sw.js v160 + APP_SHELL 含 ./src/qrcode.min.js（续 WIP，PWA 强刷）。代理端 handle115Proxy + CORS(允许 X-Proxy-Token/Cookie + POST) 前序已落 node --check 通过。校验：node --check ui-ios.js + index.js 通过；隔离测试（抽取 115 块、桩 fetch/DOM）跑通 扫码→轮询(0→1→2)→兑换→拼 cookie→存 idb→自检显示「Cookie 有效(tester)」全过。⚠️ 待用户真机 P0 验证：Vercel Worker 需 `vercel --prod` 重部署（handle115Proxy 路由就位但未上线）代理才生效；iOS PWA 强刷(sw v160)后须清站缓存重加主屏。Cookie 安全红线：仅存 IndexedDB 本机、代理不存数据。未推（等用户令「推」）。下一步：P2 iOS 云下载/上传(clouddownload.115.com + webapi 目录选择，默认「云下载」cid 3311283881428122938)+ PC 端后做。

[2026-09-05 22:51] 大S：修复「第一次点加载更多会重复加载已有的剧照」。根因：renderFilmDetail 初始渲染用 queueShot 入队 initN=getShotCap() 张剧照，但 detailShotQueueIndex 在 4403 重置为 0 后、初始入队（4417-4418）未推进游标，故首次 loadMoreShots 从 0 起又把前 initN 张重排一遍→重复加载。修复（src/ui-ios.js:4419）：初始入队后加 `detailShotQueueIndex = initN;`。sw.js v160→v162（强刷 PWA 拉新）。校验 node --check 过。⚠️ 过程纠正：首版 commit 误将工作树中用户未提交的 115 云下载 P2 代码（~92 行 WIP）一并 git add；已 git reset --soft 拆分，最终干净 commit 59ea040 仅含游标一行+sw v162，115 云下载代码已 cp 恢复回工作树（未提交、不随推送）。未推（用户自推 GitHub Desktop；origin 当前有 1 个新 commit 领先，推前需先 Pull）。

[2026-09-05 22:58] 大S：修复 PC「添加影片」弹窗 bug。根因：openCustomEdit 调 newFilm(false) 开添加影片弹窗，但 newFilm 不清 editReturnToDetail 全局标记（该标记由 detailEdit 设 true、closeEditModal 仅在 body 带 edit-modal-open 时清除）。若此前编辑过详情页影片（标记残留 true），再开添加影片弹窗并保存/关闭，closeEditModal 命中 editReturnToDetail 分支 → openFilmDetail(currentFilmId) 强制跳详情页（currentFilmId 此时为新影片 id）→ 添加完被拽进详情页而非回首页。修复：openCustomEdit（src/ui.js:1311）显式 `editReturnToDetail = false`（不能放 newFilm，否则破坏 detailEdit 的 then 仍需 true 的语义）。校验 node --check 过。提交 4bb1a1e（仅 src/ui.js 3 行；工作树 src/ui.js 仍残留非本次的 '泰坦尼克号' 占位符遗留改动，未纳入）。未推（用户自推 GitHub Desktop）。⚠️ 额外风险未修：detailEdit 异步 loadFilm 途中若用户切走页面，then 回调仍会打开编辑 modal 且标记 true，存在幽灵弹窗/误跳详情页竞态，超出本次范围，待用户确认是否修。

[2026-09-05 23:06] 大S：修复 PC「分类弹窗在编辑弹窗下方，层级不对」。根因：编辑弹窗 `#page-edit` z-index 为 500，而 `.mask`（#sheetMask）只有 400、`.modal`（分类选择弹窗 #listSheet）只有 401，所以打开分类弹窗时弹窗本体与遮罩都被编辑弹窗盖住。修复：styles/pc.css 中 `.mask` z-index 400→505，`.modal` z-index 401→510（均高于 #page-edit 的 500），同时保持 `.menu`(600) 与 `.preview-mask`(700) 仍在其上，整体层级秩序不变。提交 377e639（仅 styles/pc.css 2 行改动）。工作树中 nfo-editor-ios.html/src/ui-ios.js/src/ui.js/styles/ios.css 等用户遗留 WIP 未纳入。未推（用户自推 GitHub Desktop）。

[2026-09-05 23:14] 大S：PC 编辑页「基础信息」布局重构（按用户截图+文字要求）。改动：
1) nfo-editor.html：影片名与文件名改同行，外层 `.field-row.form-row.span-2`，影片名 `.grow-3`、文件名 `.grow-1`（3:1）；
年份/时长/评分/分级改同行，外层 `.field-row.form-row.span-2.four`，四等分。
2) 分类区：国家/地区、类型从「tag-cloud + 选择弹窗按钮」改为 `<input class=input id=countryInput/genreInput>` 直接输入，用中英文逗号分隔；输入框下方渲染 `.preset-chips`（未选中的 presets 作为 chip-add，点击加入 input）。
3) styles/pc.css：新增 `.field-row.form-row`（align-items: stretch）、`.grow-3/.grow-1` flex 比例、`.preset-chips` 样式。
4) src/ui.js：新增 `splitTags`（中英文逗号兼容、去空去重）、`onCountryInput/onGenreInput`（实时解析到 state.countries/genres）、`renderCountryPresets/renderGenrePresets`（渲染未选 presets）、`addCountryChip/addGenreChip`（点击加入并去重）；`renderCountryChips/renderGenreChips` 改为同步 input value + 刷新 presets。旧 `openCountrySheet/openGenreSheet` 保留防止外部调用报错。
提交 6c76a8d（nfo-editor.html / styles/pc.css / src/ui.js）。工作树仍保留用户 WIP：nfo-editor-ios.html、src/ui-ios.js、styles/ios.css、sw.js、iso115_test.js 及 docs/tests，未纳入。未推（用户自推 GitHub Desktop）。

[2026-09-05 23:31] 大S：修复「点击 115 网盘没反应」并推送。根因：此前 open115Sheet 漏 openSheet('sheet115') 调用，弹层从不显示（用户 9/2 反馈，本轮才真正落地推送）。本次提交 71f5ad9（4 文件 +110/-17，已推 origin/main）：① open115Sheet 补 openSheet('sheet115')，弹层必现；② 配置页改为 Cookie+二维码——进入即展示二维码、每 120s 自动刷新、带倒计时文案；兑换到 cookie 后隐藏二维码、显示「重新展示二维码」入口；③ 磁力列表每条新增复制/115离线胶囊按钮，离线固定默认目录 cid 3311283881428122938（不让选）；④ sw.js v162→v163 强刷。隔离自测全链路通过（扫码→兑换cookie→磁力一键离线默认目录）。⚠️ 生效前置：① 手机端须清站缓存/重加主屏强刷 PWA（sw 已 v163）；② Vercel Worker 需 Redeploy（handle115Proxy 路由 e42ab78 未上线则登录/离线请求会被代理拦）；用户可用 GitHub Desktop 自推的习惯本轮因未提交而漏推，已补推。未动 src/ui.js（用户 PC WIP 保留）、docs/tests/AI-COLLAB 仍 untracked 不推。

[2026-09-05 23:48] 大S：用户反馈「生成二维码失败：获取二维码失败」截图。已在前端把代理错误详情透传出来。提交 8b6b29c（src/ui-ios.js + sw.js，+34/-9，已推 origin/main）：c115ProxyFetch 保留 HTTP status 与响应体片段，非 2xx 直接抛错；start115Login / c115PollOnce / exchange115Cookie 显示 status + 上游错误文本。sw.js v163→v164 强制 PWA 拉新。⚠️ 最可能原因仍是 Vercel Worker 未 Redeploy：旧部署无 /api/115/proxy 路由，会返回 404。用户需清 PWA 缓存重加主屏后再试，并把新的错误文案截图发我确认。未动 src/ui.js（PC WIP 保留）。

[2026-09-05 23:54] 大S：用户截图「生成二维码失败：Failed to fetch」，说明 fetch 本身就被浏览器拒绝（网络层/CORS/地址错/Worker 未上线）。已再推 ba2ca12（src/ui-ios.js + sw.js）：c115ProxyFetch 网络错误会把完整请求 URL 写进错误文案；start115Login 前缀改为「连不上代理」。sw.js v164→v165。用户清缓存重加主屏后再试，错误文案会带出实际请求的 Worker URL，可一眼判断是地址填错、域名被墙还是 Worker 未部署。仍怀疑 Vercel Worker 未 Redeploy 或 vercel.app 域名在本地网络下被墙。

\n[2026-09-07 08:45] 大S：115 代理根因已定位并修复（本地未提交，待用户自推）。决定性诊断：PWA 内磁力搜索正常（vercel.app/?q= 根路径，URL 无 115），但 115 走 vercel.app/api/115/proxy?...115.com（URL 含 115）报 Failed to fetch，而 Safari 地址栏开同 URL 返回 JSON → 用户网络/科学上网拦截「URL 含 115」的 fetch 但放行顶层导航。修复：① 前端 c115ProxyFetch 改统一 POST 到新路由 /api/cloud/proxy（路径无 115），目标URL/令牌/115Cookie/POST体全放 body，不发自定义头（不触发预检）；② Worker handle115Proxy 重写从 body 读 url/token/ck/method/payload，新增 /api/cloud/proxy 路由（旧 /api/115/proxy 保留供手动 Safari 测）。隔离测试两端全过（并修掉 Worker 默认上游方法误用 POST 的 bug）。③ sw.js v167→v168（关键：不 bump 已装 v167 的 PWA 不会重拉新 ui-ios.js，修复不上线）。前端改动文件：nfo-editor-ios.html(?按钮移除)、src/ui-ios.js、sw.js；Worker 改动：api/index.js。均本地未提交。用户自推后需 Vercel Redeploy Worker 且在 115 配置页「代理令牌」填与 Vercel C115_TOKEN 一致值（或改 Vercel 环境变量回默认 C115PX_7d3k9f2m5q8x1a4t）。

[2026-09-07 10:58] 大S：修复「115 离线下载失败：decode fail!」。根因：离线接口端点用错——`clouddownload.115.com/lixianssp/?ac=add_task_url` 是 115 客户端专用「加密通道」接口，参数需设备密钥加密，明文表单 POST 过去无法解密即返回 {"state":false,"error":"decode fail!"}。修复（src/ui-ios.js c115Offline ~1755 行）：端点改为网页端 `https://115.com/web/lixian/?ct=lixian&ac=add_task_url`（明文 url=<encodeURIComponent(magnet)>&wp_path_id=<cid> + Cookie，开源油猴脚本同款写法）；magnet 先 trim；新增 errcode 10008（任务已存在）单独提示。Worker 无需改（白名单已含 115.com）。sw.js v170→v171 强刷。校验 node --check 过。未推（用户自推）。生效前置：用户推前端后强刷 PWA；本次 Worker 无改动、无需 Redeploy。

[2026-09-07 11:10] 大S：磁力交互改为「该条内联蒙版」（按用户要求），并修首次点离线误报未登录的 bug。
① 交互改造（原底部弹层方案作废）：nfo-editor-ios.html 删除 #magnetOpMask/#magnetOpSheet 底部弹层 DOM；styles/ios.css 末尾块替换为 `.magnet-inline-actions`（绝对定位铺满该条、rgba(0,0,0,.55) 蒙版、胶囊按钮 复制/115离线，`.detail-magnet/.magnet-result` 加 position:relative+overflow:hidden）。src/ui-ios.js：openMagnetOp(el) 改接**元素**（读 data-magnet / data-link，兼容详情页 data-link），在该条 append 蒙版层；再点同一条收起；closeMagnetOp 清理所有 .magnet-inline-actions；document 捕获阶段监听点击，点非当前行即收起。调用点同步：renderMagnetResults(1697) / toggleMagnetActions / detail-magnet(4566) 均改为 openMagnetOp(this|el)。
② Bug 修复：首次点「115离线」总提示「请先到设置→115网盘登录」——根因 c115Offline 只读内存 state.c115Cookie，而该值仅在 open115Sheet 里异步从 IndexedDB 回填，未进过配置页时为空。新增 ensure115Cookie()（内存没有则 idbGet('kv', C115_COOKIE_KEY) 并回填 state），c115Offline 改为 ensure115Cookie().then(...) 后再发请求。
③ sw.js v171→v172（强刷）。node --check 过。未推（用户自推）。Worker 无需改动/Redeploy。

[2026-09-07 11:45] 大S：115 自动化功能开发完成（未动 core 共享文件，仅 iOS 端）。①详情页右上角新增自动化入口（dtActAuto 按钮+autoBadge 角标，角标数=进行中/失败/等待中任务数，renderFilmDetail 时按影片重置 auto115Doc 并异步刷新角标，防跨影片残留）。②新增 #page-auto 自动化页：任务列表+展开六步时间轴（提交离线/等待离线/创建文件夹/移动视频/改名/清理），徽章大状态=失败>已中止>等待中>提交中>离线中(n/3)>整理中·步骤>已完成>待提交。③磁力内联蒙版：详情页按钮文案改「加入自动化」走 auto115Run 六步流水线；磁力搜索页保持「115 离线」一键离线。④执行参数：探测 10s/次最多 3 次转 waiting（可继续探测/重新提交），全自动连跑失败即停，每步可重试（重试=该步及后续步重置），新建文件夹名=标题建在云下载目录，视频改名 番号.原扩展名（无番号则该步 fail 提示），移动排除 sample/预告取最大视频，末步自动删磁力文件夹。⑤持久化 IndexedDB kv 'auto115:'+filmId，进页 auto115Resume 续跑。⑥sw.js v172→v173。隔离测试 tests/auto115-flow-test.js 16 用例全过（node --check 过）。仅改 iOS 文件，未推（用户自推）。

[2026-09-07 11:50] 大S：修复「点详情页磁力没反应」——根因是 styles/ios.css 里 `.magnet-inline-actions` 整块样式缺失（此前 底部弹层→条内蒙版 改造时旧样式删了、新样式未写入，JS 插入的操作层无定位/覆盖样式故视觉无反应）。已在 ios.css 末尾补齐：.detail-magnet/.magnet-result 加 position:relative+overflow:hidden，蒙版 absolute inset:0 + rgba(0,0,0,.55) + 居中胶囊按钮（复制/加入自动化|115离线）。sw.js v173→v174。HTML 无需改（14 行外链 ios.css）。未推。

[2026-09-07 12:05] 大S：⚠️ 重大事故修复——v1 自动化流程把用户 115 云下载文件夹删了。根因：StepWait 曾把 115 任务列表的 cid/wp_path_id（= 下载目标目录，即云下载根目录）当作「离线落地文件夹」存入 t.offlineDirCid，cleanup 步骤对该 cid 执行 rb/delete → 删掉云下载根目录。已按用户新流程重写为 v2：①定位文件夹（云下载目录按 user_ptime 倒序，任务名精确匹配优先、提交时间窗 ±10min 兜底；结构字段区分文件夹/文件——文件夹条目有 cid 无 fid，绝不按扩展名，因磁力文件夹名可能形如 xxx.mp4；单文件落地则 move/cleanup 自动 skip）；②清理文件（保留最大视频，优先非 sample/预告，其余全部 rb/delete 批量删，只删子项绝不删文件夹本身，根目录 cid 双重校验）；③视频改名 番号.ext（不变）；④文件夹改名为影片标题（无标题回退番号）。毒源已除（StepWait 不再写 offlineDirCid）；AUTO115_FLOW_VERSION=2，存量 v1 任务载入时自动重置步骤。tests/auto115-flow-test.js 全过（含 xxx.mp4 伪装文件夹、sample 比正片大、单文件落地、DIR999 仅作 pid 断言）。sw v175。未推。

[2026-09-07 12:12] 大S：修复「进自动化页再返回详情页底图消失」。根因：switchPage 离开详情页时调 stopDetailBgSlideshow()，该函数把 #detailPoster/#detailPoster2 两层 backgroundImage 全清空；返回详情页仅切 .active 不重渲染，底图即消失。修复（src/ui-ios.js）：①stopDetailBgSlideshow 不再清活动层 #detailPoster 的 backgroundImage（换影片时 renderFilmDetail 会立即重设新图，保留无副作用）；②新增 resumeDetailBgZoom()，switchPage 返回详情页时对保留底图恢复 5s 缓慢放大动画。sw v175→v176。node --check + auto115-flow-test 全过。未推。

[2026-09-07 12:16] 大S：自动化页按用户要求改三处（iOS 端）：①#page-auto 定位为三级页——switchPage 里 page==='auto' 时隐藏 #tabBar（style.display='none'），离开自动恢复；②顶部「目标：」行改为只显示番号（renderAuto115 内 autoFilmTitle 文案：'目标：' + (dvdId || filmTitle || '未命名')，不再带影片标题括号）；③删掉空状态提示小字 .auto-empty-sub（nfo-editor-ios.html）。sw v177。node --check + auto115-flow-test 全过。未推。

[2026-09-07 12:35] 大S：新增「上传 NFO」功能 + 自动化页文案/布局微调。①已完成任务展开后新增「上传 NFO」按钮（单文件落地任务不显示；成功后变「重新上传 NFO」，t.nfoUploaded 标记持久化）——走 115 网页端普通上传通道（sampleinitupload 无需签名）：init(uplb.115.com/3.0/sampleinitupload.php, userid=Cookie UID) → OSS multipart(name/key/policy/OSSAccessKeyId/success_action_status/callback/signature/file) → {state:true,code:0}；二进制经 /api/cloud/proxy 以 base64(b64=1) 透传（Worker 侧 Buffer.from(payload,'base64') 解码后转发，白名单放宽为 *.115.com 后缀——上传 host 由 init 动态返回）。上传 3 文件：番号.nfo / 番号-poster.jpg / 番号-fanart.jpg（loadFilm 取影片数据，buildNFOMovieXml 生成 NFO，dataUrlToBytesSync 提取图片字节），顺序上传任一失败即中断并 toast 指明文件。⚠️ Worker（api/index.js）有改动，push 后需 Vercel Redeploy 才生效。②文案：重跑失败步骤→重试、删除任务→删除、登录提示同步改；③清空已完成→清空（HTML）；④.auto-topbar-title 绝对定位全宽 text-align:center（标题真居中，右侧按钮宽度不再影响）。tests/auto115-flow-test.js 新增上传链路 8 断言全过。sw v178。均未推。

[2026-09-07 12:45] 大S：自动化新增「并入模式」（同影片多个磁力 → 同一文件夹多版本，按用户要求）。改 auto115StepRename/auto115StepCleanup：①rename 步先查 auto115FindMergeTarget（同影片下 cleanup ok/skip 且有最终文件夹的任务，返回 finalDirCid||offlineDirCid——并入过的任务用 finalDirCid，因临时文件夹已删）；命中则把视频 files/move 进该文件夹，再按「去扩展名主名」判占用选后缀：番号.ext → 番号.A.ext → 番号.B.ext（同番号不同扩展名也算占用，防多版本同名），files/edit 改名，t.finalDirCid/finalDirName 记录最终文件夹；②cleanup 步并入分支：删除已清空的临时文件夹（rb/delete，根目录双保险 offlineDirCid≠root≠finalDirCid），单文件并入则 skip；③「上传 NFO」按钮目标改 finalDirCid||offlineDirCid，显示条件放宽为 (ab-ok && (finalDirCid || !noFolder))，并入的单文件任务也能上传。独立首任务行为完全不变。tests 新增并入用例（第二/第三个磁力、番号.A/.B、临时文件夹删除、终态已完成）全过。sw v179。未推。

[2026-09-07 12:43] 大S：用户令「合并 api 配置和翻译配置。api 配置名称改为应用配置。各项分别为：元数据、激活码、AI翻译（三个输入框内的短标题改为地址/key/模型名）」。改动（iOS 端）：① nfo-editor-ios.html 设置页把「API 配置」「翻译配置」两行入口合并为「应用配置」一行；② #apiSheet 弹窗标题改为「应用配置」，在原有「元数据」「激活码」两组下方新增「AI 翻译」组，内含地址/key/模型名三个输入框（带 api-input-tag 左侧短标签）；③ 删除独立的 #translateSheet 弹窗；④ src/ui-ios.js：openApiKeySheet 回填翻译三输入框 + toggleTranslateClear；persistApiSettings 扩展为同时持久化 apiKey/activationCode/translateConfig；openTranslateSheet/persistTranslateConfig 保留为兼容入口（转调 openApiKeySheet/persistApiSettings）；设置页各处提示语「设置 → API 配置」改为「设置 → 应用配置」；⑤ styles/ios.css 注释「API 配置弹窗」改为「应用配置弹窗」。⑥ sw.js v176→v180（iOS PWA 强刷拉新）。校验 node --check src/ui-ios.js 过。处理过程：因前序 115 自动化 WIP 仍驻留在工作树（nfo-editor-ios.html/src/ui-ios.js/styles/ios.css/sw.js），本次先 stash WIP、在 HEAD 纯净版上改动并提交 ef6b90d，再 stash pop 恢复 WIP（sw.js 版本号冲突保留 v180）。最终工作树仍保留用户 WIP 未提交。未推（用户自推 GitHub Desktop）。

[2026-09-07 12:52] 大S：三项改动（iOS 端）。①详情页剧照过滤 <25KB（SHOT_MIN_BYTES）：dataURL 在列表构建期同步按字节过滤；远程 URL 在 queueShot 里 fetch(force-cache) 实测 blob.size，<25KB 走 dropShot——从 detailFullShots/currentDetailShots 剔除并按 src 重排已放置元素的 data-idx（placeShot 改为按 src 实时查索引 + detailPlacedShotEls 登记，全屏预览索引不错位），fetch 失败不拦截按原逻辑显示。②未登录 115 点磁力直接复制：openMagnetOp 先 ensure115Cookie，无 cookie → copyText 直接复制不弹蒙版；登录后才出现内联蒙版（复制/115离线|加入自动化）。③自动化任务排队串行化（auto115RunningId + queued 标记）：auto115Run/RetryStep 遇其他任务在跑 → 转排队（submit idle + msg 排队中），auto115Finish（成功/失败/中止）→ auto115AdvanceQueue 启动最早排队任务；删除正在跑的任务也释放队列；auto115Resume 会自动续跑上次遗留的排队任务。回答了用户「同时点多个 115 离线会怎样」：并发会互抢时间窗定位/同名文件夹改名冲突，排队+并入解决。tests 新增排队用例（排队标记/不提前提交/终态推进/自动续跑）全过。sw v181。未推。

[2026-09-07 12:59] 大S：用户截图要求调整 iOS「应用配置」弹窗激活码区布局：① 状态文字（已激活/未激活）放在「激活码」标题右侧，已激活用「·」前缀分隔；②「验证」按钮从底部移到激活码输入框右侧；③ 去掉 AI 翻译组底部提示语小字。改动：nfo-editor-ios.html 重构激活码区，状态 span 移入 api-group-title-row；api-input-wrap 加 activation-input-wrap，内部右侧放 verify-btn 验证按钮；删除 .activation-row 与 .api-help-note。styles/ios.css：#apiSheet .api-group-title-row .activation-status 标题右侧样式；.activation-status.ok::before { content:"· "; } 给已激活加前缀点；.activation-input-wrap .ios-input padding-right:110px，api-clear right:68px 为验证按钮腾位；.verify-btn 绝对定位在输入框右侧。sw.js v181→v182（PWA 强刷拉新）。提交 aaefdc8。未推（用户自推 GitHub Desktop）。

[2026-09-07 13:08] 大S：按截图再调 iOS「应用配置」激活码区：① 状态文字（已激活/未激活）从 title-row 右侧移到「激活码」标题内紧跟标题，用 margin-left:8px 挨紧；② 激活码输入框前加「VIP」短标签（仿 TMDB 标签样式）。改动：nfo-editor-ios.html 把 #activationStatus 移入 .api-group-title 内部；input 前加 <span class=api-input-tag>VIP</span>。styles/ios.css：#activationCodeInput padding-left 12px→46px；activation-status 选择器从 .api-group-title-row 改为 .api-group-title 并加 margin-left。sw.js v182→v183。提交 db24ac2。未推（用户自推 GitHub Desktop）。

[2026-09-07 13:48] 大S：两处小改。①自动化页「清空」按钮移到右上角：styles/ios.css .auto-topbar-clear 加 margin-left:auto（flex 推到最右，标题绝对定位居中不受影响），形成 返回(左)/标题(中)/清空(右) 三段式。②修复「上传 NFO」第二跳 403：用户实测 init(uplb.115.com) 通了但 OSS 落地 host fhnfile.oss-cn-shenzhen.aliyuncs.com 被白名单拦——api/index.js 白名单正则新增 /^oss-cn-[a-z0-9-]+\.aliyuncs\.com$/（覆盖 115 各区域 OSS 节点，不放行整个 aliyuncs.com）。⚠️ Worker 再改动，push 后需 Vercel Redeploy。sw v183→v184。两文件 node --check 过。未推。

[2026-09-07 13:53] 大S：115 Cookie 自检时机调整（用户定调）。①bootApp 启动时若 IndexedDB 有已存 Cookie → 自动后台自检一次（ensure115Cookie → verify115(true)），结果不写设置页状态行；确认失效（Cookie/失效/令牌/登录类错误）才 toast「115 Cookie 已失效，请到设置重新登录」，网络类错误（Load failed 等）完全静默。②open115Sheet 打开 115 配置页不再自动 verify115（原 1130 行删除），状态行仅由「自检」按钮手动触发；扫码登录成功（1301 行）与手动保存 Cookie（1324 行）后的 verify 属用户操作即时反馈，保留。verify115(silent) 改为 silent 模式不碰 #c115Verify DOM。sw v184→v185。node --check + auto115-flow-test 全过。未推。

[2026-09-07 14:10] 大S：Worker 白名单再补一条精确条目 fhnfile.oss-cn-shenzhen.aliyuncs.com（115 上传实测返回的 OSS bucket，作为正则之外的保险）。⚠️ 关键问题仍是部署未生效：浏览器带 token 探测仍返回白名单错误 → 线上为旧代码。用户已确认前端无 Worker 地址输入（固定走 DEFAULT_WORKER）。待用户在 Vercel 控制台确认最新部署的 Production/Preview 徽章与生产分支设置。node --check 过。未推。

[2026-09-07 14:15] 大S：上传 NFO 白名单已过（Vercel 部署生效），错误前进到 115 业务层：state=false code=990002（参数错误）。对照 SheltonZhu/115driver 与 115-sdk-go：OSS POST 回调参数应为 callback + callback_var 成对，且 init 返回的 d.callback 常是 {callback, callback_var} 对象——原代码直接把对象字符串化进表单（[object Object]）且漏传 callback_var → 115 回调校验失败 990002。修复（src/ui-ios.js c115UploadFile）：d.callback 兼容对象/字符串两态，callback_var 存在则以独立表单字段传；multipart 报错增强（带出 errno/message）。sw v185→v186。node --check + auto115-flow-test 全过。未推。

[2026-09-07 14:20] 大S：离线探测节奏改为递增间隔（用户要求）：AUTO115_PROBE_MS(固定10s) → AUTO115_PROBE_GAPS=[5000,10000,20000]，第1次探测前等5s、第2次前10s、第3次前20s，共3次约35s内完成（原3次需30s且首查偏慢）。auto115ScheduleProbe(t) 按该任务 wait.probes 取下一档间隔；无 t 的群体调度（auto115Resume）取所有 running 任务最早到期者。新增 auto115ProbeDelay(doneProbes) helper。AUTO115_PROBE_MAX=3 不变。sw v186→v187。node --check + auto115-flow-test 全过。未推。

[2026-09-07 14:22] 大S：修正探测节奏（用户澄清）：探测时刻为提交后 5s/10s/20s（累计 20s 内探完 3 次），即间隔 5s→5s→10s。AUTO115_PROBE_GAPS 由 [5000,10000,20000] 改为 [5000,5000,10000]（src/ui-ios.js:1831，auto115ProbeDelay 按 probes 索引取值逻辑不变）。sw v187→v188。node --check + auto115-flow-test 全过。未推。

[2026-09-07 14:26] 大S：上传 990002 二次修复——OSS callback 需 base64。对照 SheltonZhu/115driver（Go）：oss.Callback/oss.CallbackVar 传参前均 base64.StdEncoding.EncodeToString，即阿里云 OSS 规范要求 callback/callback_var 表单字段为 base64(JSON)。v186 修复只补了 callback_var 字段但仍发明文 → OSS/115 回调解析失败 990002「参数错误」（用户 14:22 实测确认）。修复（src/ui-ios.js）：新增 c115OssCallbackField(v)——明文 JSON（{开头）→ UTF-8 安全 base64；字符串（老协议，Fake115Upload 原样传法）→ 视为已编码原样。c115UploadFile 对对象形态的 callback/callback_var 取值后统一过该函数。sw v188→v189。node --check + auto115-flow-test 全过。未推。

[2026-09-07 14:36] 大S：上传 990002 排查转入「拿 ground truth」策略。调研结论：①xpmibackup_sly（Cookie 直连、近期实测可跑）证实 sampleinitupload 流程 init 只 POST filename+target、返回 callback 为 base64 字符串、multipart 仅 key/policy/OSSAccessKeyId/signature/callback/file 且不传 callback_var——与我们现状基本一致，callback 编码处理理论已对；②Alibaba 官方文档证实 PostObject callback 表单字段=base64(JSON) 已符合；③盲改无意义。故 v190 改动：①c115OssCallbackField 加固——对象值先 JSON.stringify 再判 base64（防 [object Object]）；②上传失败报错自诊断——toast 带版本标记 v190 + init 返回的 callback 形态（none/str:前14字符/obj:键名）+ OSS 原始回包前110字符（c115UploadMultipart 新增 dbg 参数）。用户重试后截图即可定案（若 toast 无〔v190 标记 = PWA 未刷新）。sw v189→v190。node --check + auto115-flow-test 全过。未推。

[2026-09-07 20:40] 大S：PC「新建影片」弹窗布局优化（按用户两则请求）。改动（nfo-editor.html / styles/pc.css / src/ui.js）：① 删除独立的「海报」tab（edit-tabs 按钮 + #tab-poster 整块），将其 poster/fanart/logo 三个 media-drop 上传块移入「媒体与预告」(#tab-media) 的 .poster-grid（3 列网格样式由 `#tab-poster .poster-grid` 改为 `#tab-media .poster-grid`）；移除预告片输入框(id=trailer)与字幕资源说明段(p class=t-small)，保留「包含字幕」开关。② 基础信息布局：影片名整行；原始标题(1/2,grow-2)/文件名(1/4,grow-1)/首播日期(1/4,grow-1) 同一 field-row；年份/时长/评分/分级四等分保持；去掉【附加信息】标题，番号(1/7)/系列(2/7)/制作商(2/7)/发行商(2/7) 改入 .av-grid-seven（grid 1fr 2fr 2fr 2fr，#avFieldsCard 保留供 applyEditMode 钩子）；去掉【分类】标题（国家/地区+类型字段及 preset-chips 保留）。③ 小圆角：#page-edit 的 .input/.select/.textarea 统一 border-radius: var(--radius-xs)（仅输入框，按钮/卡片不变）。④ 修 switchHomeTab 删掉已不存在的 #tab-poster 引用（避免 getElementById 返回 null 后的潜在错误）。⑤ nfo-editor.html ?v=44→?v=45（5 处强刷）。校验：node --check src/ui.js 过；core.js:611/769 的 getElementById('trailer') 本就有 if(tEl) 守卫，删除 #trailer 后静默跳过、state.trailer 仍正常保存，无回归。未推（用户自推 GitHub Desktop）。

[2026-09-07 14:46] 大S：AI 翻译标题剥括号（用户要求）：「泉まりん（泉玛丽恩）」→「泉まりん」。core-shared.js 新增 stripTitleParens(s)（剥全角（）与半角 () 最内层内容 + 收敛连续空格，仅用于 title、不碰 plot），translateMeta 里 title 清理链改为 stripTitleParens(cleanTranslatedText(...))，并导出到 NfoCore；PC core.js 内联 translateMeta 的 title 同步过 NfoCore.stripTitleParens（313 行）。验证输出与用户示例逐字一致；auto115-flow-test 全过。改动并入未推的 v190。

[2026-09-07 14:55] 大S：上传 990002 第三次修复——按可跑实现精确重写请求形态。诊断 toast（v190）拿到 ground truth：init callback 为 str 形态（eyJjYWxsYmFja1… = base64 of {"callbackUrl":…}，我们原样传没问题），OSS 接受上传但 115 回调注册失败回 {"state":false,"message":"参数错误"}。对照近期实测可跑的 suileyan/xpmibackup_sly Pan115Provider.java，与我们请求有 4 处实质差异：①init 只 POST filename+target（我们多传了 userid+filesize）；②multipart 精确 6 字段 key/policy/OSSAccessKeyId/signature/callback/file（我们多传 name+success_action_status，且签名顺序不同）；③响应兼容包在 data 里的形态；④callback 无 callback_var（base64 串内已含完整回调配置）。已照参考实现重写 c115UploadFile（删除 c115GetUserId），版本标记 v191。tests 更新断言（init 无 userid/filesize、multipart 精确字段集、file 在最后）全过。sw v190→v191。未推。

[2026-09-07 15:22] 大S：上传 990002 第四轮——v191 实测仍失败（用户 toast 确认），但排除了表单字段嫌疑：拉取 Fake115Upload 源码发现其 sampleinitupload 流程 = v188 形态（init 带 userid+filesize、multipart 带 name+success_action_status）且能注册成功 → v188/v191 两套字段形态都有人跑通，问题不在字段。剩余唯一大差异 = User-Agent：可跑实现用 115disk/11.2.0（Fake115Upload，init+OSS 均用）或 115Browser/23.9.3.2 + aliyun-sdk-android/2.9.1（xpmibackup_sly），而我们经代理转发的是 iPhone Safari UA（浏览器 fetch 禁设 UA 头，此前无法覆盖）。v192 改动：①Worker /api/cloud/proxy 支持 form.ua 覆盖 User-Agent（api/index.js:892）；②前端 init 请求 ua=115disk/11.2.0、OSS multipart ua=aliyun-sdk-android/2.9.1；③诊断升级：toast 带 st=init status（1=OSS/2=秒传）+ http=OSS 响应状态码（203=文件已存但回调注册失败）。⚠️ Worker 有改动，push 后需 Vercel Redeploy。sw v191→v192。node --check ×2 + auto115-flow-test 全过。未推。

[2026-09-07 15:50] 大S：⚠️ 上传 NFO 通道整体重写为 115 4.0 协议（v193）。定性结论：3.0 sampleinitupload 老通道已被 115 废弃——v191（对齐 xpmibackup_sly）与 v192（UA 覆盖）两轮实测均止步于「OSS 收下文件、回调注册回 state:false 参数错误」；T3rry7f/Fake115Upload issue#25 证实 115 强制升级 4.0，维护中的 orzogc/fake115uploader 已整体改走 4.0。已逐行移植其实现（src/ui-ios.js 上传区整体替换，~330 行）：①proapi.115.com/app/uploadinfo 取 user_id/userkey；②SHA1(file)=fileID，sig=SHA1(userkey+SHA1(userID+fileID+target+"0")+"000000")大写，token=MD5(Qclm8MGWUv59TnrR0XPg+fileID+fileSize+signKey+signVal+userID+t+MD5(userID)+30.5.1)；③表单 AES-CBC 加密（ECDH secp224r1 与 115 固定公钥协商，共享密钥按 Go big.Int.Bytes() 语义去前导零后取首16字节为 key/末16字节为 iv）→ POST uplb.115.com/4.0/initupload.php?k_ec=<EncodeToken(时间戳+pubKey变换+CRC32,48B)>；④响应 AES 解密→[2B LE 块长+LZ4块] 解压→JSON；status=2 秒传命中 / 7+701 按 sign_check("start-end"闭区间) 补切片 SHA1 重提 / 1 → getuploadinfo→gettokenurl 取阿里云 STS；⑤OSS PUT endpoint/bucket/object，x-oss-security-token + x-oss-callback(-var base64) 头 + OSS V1 HMAC-SHA1 签名 Authorization。配套：c115ProxyFetch 加 xs(附加头)/bin(二进制响应 base64) 模式；Worker 白名单加 proapi.115.com + 转发 xs 头（⚠️ 需 Vercel Redeploy）。加密层含纯 JS secp224r1（BigInt 仿射）/MD5/CRC32/LZ4 解码器。tests 全重写上传段：MD5/CRC32 标准向量、P-224 曲线与 ECDH 一致性、LZ4 两路解码、4.0 全链路（mock 用同会话密钥加解密，验证加密表单字段/OSS 签名头/PUT URL）全过。sw v192→v193。未推。

[2026-09-07 17:04] 大S：用户令「翻译完成后，详情页简介动态刷新」。改动（PC + iOS 两端对称）：① 两端 `src/ui.js`/`src/ui-ios.js` 各新增 `refreshDetailPlot()`——仅当 `currentDetailFilmId` 等于正在翻译的影片 id 时，`loadFilm` 取最新记录并就地更新 `detailPlot` 文本 + onclick（复制简介）+ `updateTranslateRetryBtn()`；不重渲染整页（避免底图/滚动闪烁）。② 两端 `startFilmTranslation` 成功分支（`saveFilm` resolve 后、紧跟 `renderOverview()`）新增 `if (currentDetailFilmId === id) refreshDetailPlot();`，覆盖自动翻译（保存后 flushPendingTranslate）+ 手动重试（retryTranslate → startFilmTranslation）两类完成路径。③ PC `nfo-editor.html ?v=45`→`?v=46`（5 处强刷脚本）；iOS `sw.js` `nfo-ios-v194`→`v195`（PWA 强刷拉新 ui-ios.js）。校验：node --check 三文件（ui.js/core.js/ui-ios.js）全过。未推（用户自推 GitHub Desktop）。注：仅刷新「简介」，详情页标题（AV 显番号、TMDB 已多为中文）未动，符合用户「刷新简介」的明确诉求。

[2026-09-07 17:25] 大S：v193 真机报「The operation failed for an operation-specific reason」定位 + v195 诊断/自愈增强。定性：该文案 = WebKit `OperationError` 默认消息，是 `crypto.subtle`（SHA1/HMAC/AES-CBC）失败时的通用报错，无任何细节——4.0 链路里最可能是 ①initupload 回包 AES 解密失败（ECDH key/iv 派生约定与真实服务端不一致）或 ②Worker/上游错误 JSON 被前端当密文去解密。改动（仅 src/ui-ios.js 上传加密区，与 17:04 的翻译简介改动不重叠）：① c115Sha1Hex/c115AesCbc/c115HmacSha1B64 全部包 try/catch，报错带步骤标签+key/iv/data 长度；② c115EcdhGet 改为双派生变体 {variants:[strip(去前导零), fixed28(固定28字节)], active}——解密失败自动切换变体并命中 JSON 判据后，会话级切换 + c115InitUpload 用新派生重发一次表单（自愈）；③ c115EcdhDecrypt 先按明文试读（服务端明文 JSON 错误直接透出）；④ c115InitUpload 增加 res.ok 检查（Worker 502/403 等不再变成 OperationError）；⑤ OSS PUT 失败报错带 HTTP 状态码；⑥ 上传错误标记〔v193〕→〔v195〕（对齐 sw 缓存版本）。sw v195 由 17:04 翻译改动已升，本次不再动。tests/auto115-flow-test.js mock 适配 variants 结构，全过。未推（用户自推）。⚠️ 本轮参考源码（orzogc/fake115uploader cipher.go）因安全审批未读成，双派生回退是对「约定不一致」假设的兜底；下次真机报错若仍失败，toast 会带具体步骤+长度，可精确定位。

[2026-09-07 17:45] 大S：**v195 真机报错根因确认 + v196 修复**（本轮成功读到 orzogc/fake115uploader cipher.go 全文对照）。根因：真机 toast ctLen=156 不是 16 的倍数，而 WebCrypto AES-CBC 解密要求密文长度为 16 的倍数 → OperationError。参考实现 cipher.go `EcdhCipher.Decrypt` 第一步就是 `cipherText[:len-len%16]` **截断到 16 的倍数再 CBC 解密**（真实 115 回包 = 144 字节密文 + 12 字节尾巴）。同时确认：①共享密钥 = aead/ecdh 固定 28 字节（key=secret[:16], iv=secret[12:28]）→ 上一轮的 fixed28 变体即正确约定，已设为首选；②解密后结构 [2B LE lz4长][lz4块] 与 EncodeToken 字节布局均与我们的实现一致。v196 改动（src/ui-ios.js）：①c115EcdhDecrypt 先按明文试读→截断→subtle CBC（PKCS7）→失败再走纯 JS CBC 裸解兜底（不校验填充，与 Go cipher.NewCBCDecrypter 语义一致）；②新增纯 JS AES-128 块解密（S盒 GF(2^8) 运行时生成避免手抄错，FIPS-197/NIST SP 800-38A 向量验证）+ c115CbcDecryptManual；③variants 顺序 fixed28 优先、strip 兜底；④错误标记〔v195〕→〔v196〕。sw v195→v196（PWA 强刷）。tests 新增 S盒/FIPS-197/NIST CBC/截断 4 用例，全套通过。未推（用户自推）。RSA 相关（RsaCipher/gKeyL/gKts/genKey）在本轮未用到，若后续 sign_check 分片上传报错再对照。

[2026-09-07 18:20] 大S：**v197 —— 402「userid/filesize/target/pickcode invalid」处置**（仅 src/ui-ios.js 上传区）。排查过程：真机 v196 报 115 服务端业务错 status=4/statuscode=402，说明解密链路已通、服务端能加密回包；对照 orzogc/fake115uploader fast.go + SheltonZhu/115driver upload.go 两份参考，表单字段/sig/token/target 公式与我们的实现完全一致（115driver 仅多 topupload、appver 不同），加密端 = 标准 CBC+PKCS7 与 WebCrypto 等价。剩余嫌疑：服务端解表单用的 key/iv 派生与它加密回包用的不一致（解表单失败→必填字段全解析不到→恰好报这串字段名）。v197 改动：①initupload 表单补 topupload=true（对齐 115driver）；②请求带 Content-Type: application/x-www-form-urlencoded（走 Worker xs，两份参考均有）；③402 时自动换另一套 ECDH 派生（fixed28↔strip）重发表单一次（encTried 去重，最多 4 次尝试）；④初始化失败报错带 userid/filesize/target/filename 诊断值（非敏感）。402 若因 cookie 不完整（参考 #85：部分账号需 KID 字段）或海外 IP（Worker 出口）所致，重发不会自愈，但下一轮报错带字段值+〔v197〕标记可继续定位。sw v196→v197。tests 全过。未推（用户自推）。Worker 无改动。

[2026-09-07 18:40] 大S：**v197 真机反馈 = initupload 已通过！** 403 文案「HTTP 403」（无 initupload/OSS 前缀）出自 c115ProxyFetch 非 bin 分支（ui-ios.js:1104），上传链路中只有 getuploadinfo.php / gettoken.php 走该分支 → **402 关卡已过（v197 换派生重发或 topupload/CT 生效）**，现在卡在 initupload 之后的 STS 凭证获取（uplb 3.0 GET 端点，115 返回 403 空 body）。参考 oss.go：getuploadinfo/gettoken 就是普通 GET（UA+Cookie），调用方式无差异 → 403 属环境性（IP/UA/风控，今日反复测试或 Vercel 海外出口触发 115 对 3.0 端点的限制嫌疑最大）。v198 改动（仅 src/ui-ios.js）：getuploadinfo/gettoken 各自 GET 失败自动改 POST 重试一次，报错带 [getuploadinfo]/[gettoken] 端点标签。sw v198。tests 全过。未推。若下轮报错带标签仍 403 → 是 115 对该端点的 IP/风控限制，Plan B：①等待 30 分钟再试（风控冷却）；②Worker 侧去掉这两个请求的 Referer/Origin 或换 UA 再试；③查 115driver 是否有等价的非 3.0 凭证端点。

[2026-09-07 18:55] 大S：**v199 —— getuploadinfo 403 的 Worker 侧修复（Plan B）**。确认链：真机裸「HTTP 403」无 error 文本 → Worker 所有 403 都带 {error:...}（api/index.js:860/884）→ 排除 Worker 自拒，是 115 直接拒绝 uplb 3.0 GET。又因 alist 用户在各类 VPS（含海外）上该端点长期可用 → IP 嫌疑降级；对照参考 oss.go（getuploadinfo/gettoken 仅发 UA+Cookie，无 Referer/Origin），而我们 Worker 给所有转发固定加 Referer/Origin https://115.com → 升为首要嫌疑。改动：①Worker（nfo-magnet-proxy-vercel-main/api/index.js:901）支持 form.noRef=1 跳过 Referer/Origin；②前端 getuploadinfo/gettoken 带 noRef:1（GET→POST 重试保留）；③标记〔v199〕+ sw v199。tests 全过。⚠️ 本轮需要两仓库都推 + Vercel Redeploy 才生效。若 noRef 后仍 403 → 剩余变量只有 UA 差异（我们 accept/accept-language 浏览器头 + 115disk UA vs 参考 Go 默认头），下一步在 noRef 同时裁剪 accept 头或换 UA。

[2026-09-07 19:50] 大S：**v200 —— GET 改 POST + Worker 上传链路默认兜底（双仓库）。根因重判：v199 noRef 已部署但仍 403 + 对照 fake115uploader Go 客户端（只发 UA+Cookie）+ 115driver（UA=aliyun-sdk-android/2.9.1）→ 不是单变量问题，是 115 服务端对 Worker 出口（Node fetch）+ 浏览器指纹的反爬。Worker 端对 uplb/proapi/oss-cn-* 上传链路默认应用 Go 客户端请求模型（UA=115disk + 删 Accept/Accept-Language/Referer/Origin）；前端 getuploadinfo/gettoken 由 GET 改 POST（POST 是 4.0 标准调用，GET getuploadinfo 是兼容层甄别更严）+ 强制重试。标 〔v200〕，sw v200。tests 全过。⚠️ 本轮已超出单 push→重启→重测节奏，需要心理预期：「上传 NFO」反复 403 的根因可能是 115 WAF 对 Vercel IP 段或 Worker TLS 指纹的策略性拦截——这是协议逆向工程的常态边界，不是 bug。如果 v200 仍 403：Plan A 等明早 115 风控自动解除再试；Plan B 改走纯 web 端 add_file（不同风控池）；Plan C 暂时暂停上传 NFO，该功能非阻塞——自动离线 + 磁力搜索已覆盖 90% 上传需求。

[2026-09-07 20:25] 大S：**删除影片时一并清除 115 自动化记录**（src/ui-ios.js:4122 deleteFilm；sw v201）。改前 deleteFilm 只删影片 kv（`idbDelete('kv', filmKey(id))`），自动化记录按 filmId 存另一份 kv（`auto115Key(id)` = AUTO115_PREFIX + filmId）不会跟着删 → 影片删掉后其自动化任务成为孤儿（自动化列表/已完成项还能点开、上传 NFO 会指向已删影片）。改为：deleteFilm 内先清内存（auto115Doc===id 时置 null + auto115RunningId 清空），再 Promise.all 同时删影片 kv 与自动化 kv（无记录时 catch 忽略）。因为改在 deleteFilm 内层，5 个调用点（详情页 confirmDelete 6152 / 列表 6216、6226 / 批量 6525 / 过期清理 6550）全部自动覆盖，无需逐处改。tests 全过。未推。

[2026-09-07 22:10] 大S：**v202 —— 上传改走 115 开放平台官方通道（双仓库，需推 + Vercel Redeploy）**。
背景：逆向 4.0 通道（uplb.115.com）卡 403 一整天（v190→v200 十版），根因是 115 对 Worker 出口（Node fetch TLS 指纹 + 海外 IP）做客户端甄别，属反爬边界不是 bug。调研发现 115 有官方开放平台（proapi.115.com），只认 Bearer token、无指纹校验、表单明文。
**关于 client_id（重要澄清）**：实名入驻只是为了申请「自己的」client_id；**借社区公开 AppID 即可，无需入驻**。p115client 官方收集了 40+ 个有效 AppID（const.py OPEN_APP_IDS），我们实测 100195137(VidHub) 的 authDeviceCode 返回 state:1。
**授权方式（授权码模式，不扫码、不新建登录页）**：向 OpenList 公开服务 api.oplist.org/115cloud/requests 取 authorize URL（text 前缀固定 38 字符，截掉后 unquote 得 passportapi.115.com/open/authorize?client_id&redirect_uri&state&response_type=code）→ 带用户现有 Cookie 请求、redirect:manual → 已登录则 302 回回调并在 Location 带 access_token/refresh_token；未登录则 302 到 115 登录页（据此判 needLogin）。实测未登录时确实跳登录页，反证带 Cookie 就能静默通过。token：access 2h、refresh 1 年。
改动：
- Worker（nfo-magnet-proxy-vercel-main/api/index.js）：新增 handleOpen115Auth + 路由 POST /open115/auth（body: token + ck），重定向解析兼容「Location query 明文」与「base64(location[1:])」两种格式。**必须 Redeploy 才生效**。
- 前端（nfo-editor/src/ui-ios.js）：新增 C115_OPEN_KEY/c115OpenForm/c115OpenReq/c115OpenAuthorize/c115OpenToken/c115OpenUploadFile；上传三步 = POST proapi.115.com/open/upload/init（file_name/file_size/target=U_1_<cid>/fileid=SHA1/preid=前128KB SHA1/topupload=0，二次签名 code700|701 时补 sign_key+sign_val）→ GET /open/upload/get_token（一步拿 endpoint+AK+SK+STS）→ OSS PUT（V1 HMAC-SHA1，x-oss-callback/-var）。**旧 4.0 链路改名为 c115UploadFileLegacy 保留为回退**，c115UploadFileAsync 改为「先开放平台、失败回退 4.0」。
- HTML：115 配置页新增「开放平台上传 → 授权」按钮 + 状态行（open115Sheet 时 refresh115OpenStatus 回填）。
后续可删：ECDH/AES-CBC/LZ4/sig/k_ec 那约 330 行加密代码（待开放平台实测稳定后清理）。sw v202，标记〔v201〕（沿用，未改）。tests 全过。未推（用户自推）。

[2026-09-07 22:25] 大S：**v202 真机授权失败修复**（仅 Worker）。用户截图「授权失败：未能从回调中解析 access_token」。根因：实测 OpenList 动态授权链是两步 302——authorize → `api.oplist.org/115cloud/callback?code=…`（不含 token）→ 第三方服务端做 token 交换 → 302 到 `X<base64(token)>`。原版只 fetch 第一次的 location，自然拿不到（停在「/115cloud/callback?code=…」，既无 access_token query 也不是 base64）。修复：for 循环最多 5 次手动跟随重定向，每步同时尝试「URL query 解析」+「b64(location[1:]) 解析」，命中即 break；遇 needLogin（跳 115 登录页）或回到 passportapi 授权入口则提前返回 needLogin=true。前端未改（nfo-editor sw v202 已就位）。用户操作：推 Worker 仓库 + Vercel Redeploy 即可。

[2026-09-07 22:30] 大S：**v202 授权真机第二次失败修复**（仅 Worker）。截图错误「Failed to parse URL from /#eyJtZXNzYWdlI...」根因：OpenList 最终回调是 `/#<base64>` 相对路径形式 + fragment 携带 base64。前版 fetch 失败是因为 ①`new URL(location)` 解析相对路径缺 base 抛错；② base64 解出 JSON 实际是错误响应（`{"message":"..."}`），没识别还当成未命中继续跟随 → 第三次 fetch 抛 Failed to parse URL。修复（api/index.js:984-1047）：①跟随重定向前把相对路径补成绝对 URL（`new URL(location, nextUrl).toString()`）；②尝试解析 u.hash（OpenList /#/X<base64> 形式）与直接 location.slice(1) 两种 base64 格式；③识别解出 JSON 含 message/error 字段就当终态返回（携带原始 message）；④v2 跟随时不再用 location（相对）而是 absoluteUrl。Worker commit 4a16d50。未推（用户自推）。前端未动。

[2026-09-07 22:21] 大S：**v202 授权第三轮失败根因定位完成**。用户截图「授权链路把请求重定向回 passportapi 授权入口」（hint=step:needLogin=isPassport=false），即 redirectTo 含 passportapi.115.com/open/authorize 而非 115.com 登录中间页 → 用户 Cookie 已被 passport 认可，但 app_id `100197303`（OpenList 动态服务给）触发了 passport 的「再次确认授权」分支，形成重定向循环。

诊断要点：
- 占位 Cookie 测 client_id=100195313（CloudDrive2）→ 跳 115.com 登录页（符合预期，占位无效）
- 用户真 Cookie 测 client_id=100197303（OpenList）→ 跳回 passportapi 授权入口（**非登录页** = Cookie OK，是 app_id 被 passport 额外要求）
- 占位 Cookie 测 client_id=100197303 → 也跳 115.com 登录页（占位无效无法验证）

**明早方案（不依赖 OpenList）**：从 p115client const.py OPEN_APP_IDS 取第一个 app_id（p115client 默认用那个，是 115 passport 完全认可的）+ redirect_uri 用 CloudDrive2 公开回调 `https://redirect115.zhenyunpan.com`。Worker handleOpen115Auth 改造后，单跳（authorize → b64(token)），无需 OpenList 中转。

需要背景拉取的命令：
```bash
curl -sL --max-time 60 --retry 3 -o /tmp/p115const.py "https://raw.githubusercontent.com/ChenyangGao/p115client/main/p115client/const.py"
grep "OPEN_APP_IDS\|app_id" /tmp/p115const.py
```

[2026-09-07 22:30] 大S：**v202 授权第四轮修复 —— 弃用 OpenList 改用 CloudDrive2 静态组合**（Worker 545dc7b）。查 p115client 文档（p115client.readthedocs.io/en/latest/example/01.授权登录open应用.html）发现两个实测可用的组合：
- openlist (100197303) + OpenList 动态服务 → 这就是我们当前用的，但 115 passport 对它额外要求「再次确认授权」形成重定向循环
- **clouddrive (100195313) + redirect115.zhenyunpan.com** → p115client 文档明示是经过实测验证的标准组合，响应形式 `?access_token=...&refresh_token=...&state=...`（直接 query 明文，无需 base64 解码）

改动：handleOpen115Auth 删掉 OpenList 动态服务那段（fetch api.oplist.org/... → parse text.slice(38)），直接构造 `https://passportapi.115.com/open/authorize?client_id=100195313&redirect_uri=https%3A%2F%2Fredirect115.zhenyunpan.com&response_type=code&state=...`。跟随重定向逻辑保留（CloudDrive2 也会经过 callback 中转一次），但 query 明文 token 路径会先命中。

⚠️ 注意同一 app_id 最多 3 个并发授权，第 4 个踢最早的 —— CloudDrive2 是热门 app 风险中等；如果你账号上同时有 CloudDrive2 在用，可能要重启那边。后面稳定了可以再加 OPEN_APP_IDS 表的二级 fallback。

需要用户推 Worker 仓库 + Vercel Redeploy。前端不用动。

[2026-09-07 22:35] 大S：**修复 115 自检一直卡在「连通性自检中…」**（src/ui-ios.js:1343 verify115；sw v203；commit f3ecd1a）。根因：verify115 的 .then() 成功分支只做校验、**不回写状态行**（注释写的是「后台自检通过：静默即可」），只有 .catch() 才写 vEl → 手动点自检时请求明明成功（用户反馈「115 离线能正常用」证明链路通），状态行却永远停在「自检中…」。修复：成功分支在 vEl 存在时写入 `✓ 连接正常` + class `c115-verify ok`（样式已存在 styles/ios.css:1220）；silent 模式 vEl 为 null 仍保持静默。另加 15s 超时兜底（setTimeout + timedOut 标志位）：c115ProxyFetch 全项目无 AbortController/timeout，代理无响应时 Promise 永久挂起也会卡死状态行，超时后提示「✗ 自检超时（代理无响应）」且后续 then/catch 不再覆盖。tests 全过。未推。

[2026-09-07 22:40] 大S：**v202 授权第五轮修复 —— 两处协议细节错误**（Worker 9cd5abc）。用户提出关键质疑「115 是不是不允许第三方网页端，只允许第三方客户端？」→ 顺着这个方向复核 p115client 文档，发现我们两处实现违背了协议：
① **入口错**：115 的授权入口是 `https://115.com/?ac=open&redirect_uri=<urlencoded authorize>`（OpenList 返回的 text 就是这个形式），我们先前直接打 passportapi，缺了「确认授权」这层会话建立，被 115 弹回。
② **state 不是 URL**：p115client 文档 CloudDrive2 示例的 state 是 `http://localhost:19798/`（合法 URL）。CloudDrive2 中转端（redirect115.zhenyunpan.com）拿 code 换完 token 后要 302 到 state 并把 token 拼在 query 里；我们传 `nfo-editor-<ts>` 这种非 URL 字符串 → 中转端无法构造跳转 → 链路断。
连带修正：终止判断原来一见 `115.com/?ac=open` 或 `passportapi/open/authorize` 就当失败 —— 但这俩都是**正常中间跳转**，会误杀。改为记录 visited 集合，只有「回到已访问过的 URL」才判定真循环；无 location 时回读 body 前 200 字符辅助诊断。跟随上限 5→8（链路实际最多 4 跳）。
未推。用户需推 Worker + Vercel Redeploy 后再测。

[2026-09-07 22:48] 大S：**真机实测推翻 22:40 的「入口必须 115.com/?ac=open」判断**。用户给了真实 115 Cookie 在真实环境跑通：直接打 `passportapi.115.com/open/authorize`（带 Cookie）两跳即拿 token（passportapi authorize → 302 到 redirect115.zhenyunpan.com?code=… → 中转端 302 回 state 并附 access_token/refresh_token）。而 `115.com/?ac=open` 实测是 Vue 交互式登录页（title「注册/登录115生活授权」，loginState 永远为 0），**不接受已有 Cookie 静默授权、必须先输账密** —— 这正是之前所有授权失败的真根因（不是入口层、也不是 state 非 URL）。修复（Worker 745ef7b）：入口改 `startUrl = authorizeUrl`（直接 passportapi），删掉 115.com 登录壳；补 cookie jar 回带 WAF 的 acw_tc；UA 改浏览器。真机另测：① 上传通道全通——init→get_token→OSS PUT→回调 state:true，文件已落 115 根目录（测试文件 `nfo-editor-upload-test-1788792359715.nfo` 需用户手动删，开放平台无删除接口，候选路径全 980006）；② 海外 Vercel 可达 passportapi/proapi（WebFetch 实测拿到授权页/接口响应，之前被拦的只是 uplb 域）。未推。用户需推 Worker + Vercel Redeploy，PWA 重开即走新授权逻辑（前端 c115OpenAuthorize 不用动）。

[2026-09-08 07:45] 大S：**115 防风控两项改动**（src/ui-ios.js + tests/auto115-flow-test.js + sw.js；commit 68cefef）。用户先问「要不要加动态延迟防封号」，评估后结论是**不加全局延迟**——请求峰值 < 1 req/s（上传 3~4 请求、自动化 ~8 请求跨 20s+、轮询 1.8s/次），且此前所有 403 都是 IP/通道层面而非频率。改为两项针对性改动：
① **自适应失败退避**（c115ProxyFetch 统一出口，1066 行前新增 `c115FailStreak` / `c115AdaptiveDelay` / `c115Sleep`）：**成功即清零、正常路径零延迟**，只有连续失败才 800ms→1.6s→3.2s→6.4s + 0~300ms 抖动。c115ProxyFetch 由 `function` 改 `async`（`return Promise.reject` 改 `throw`，语义等价）。放在统一出口的好处：20 处调用点全覆盖，Legacy 若恢复也自动生效。
② **停用 4.0 逆向回退**：`c115UploadFileAsync` 只调 `c115OpenUploadFile`，不再 catch 回退 `c115UploadFileLegacy`（逆向 uplb 4.0 在海外必 403 = 白撞 WAF，且逆向协议是风控重点）。**Legacy 函数保留未删**，注释写明恢复方式。
- 测试适配（重要）：6b 段由「4.0 加密链路」改为「开放平台链路」——预置 `ctx.state.c115Open={access:'TEST_OPEN_TOKEN',exp:...}` 避开 refreshToken；mock `open/upload/init`、`open/upload/get_token`、OSS PUT；新增断言「未回退 4.0（调用 0 次）」。**PUT URL 断言改 OSS 子域名风格** `BKT115.oss-cn-test.aliyuncs.com/OBJ/123`（开放平台= bucket.endpoint/object，4.0= endpoint/bucket/object，别再按旧风格写）。全部通过。
- sw.js CACHE v203→v204。未推（用户自推）。
- 另梳理了 115 流程限制清单（开放平台无删除接口 / uplb 海外 403 / 依赖 CloudDrive2 app_id+中转 / 双凭证 / Worker 单点），详见当日 memory 日志。
