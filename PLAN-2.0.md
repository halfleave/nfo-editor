# NFO 编辑器 2.0 规划（规划稿 v3，待确认后开发）

> 状态：**规划稿 v3，未开发**。等你确认后下令再实施。
> 基准代码：`/work/new-nfo-editor-main/`（已同步至 `/work/nfo/nfo-editor-ios.html`，5410 行）
> 关键前提：里模式 = **进入后解锁内容/功能**（隐藏解锁式，设置标题连点 3 次）。

---

## 一、里模式范围定义（已确定）

进入里模式后解锁：
1. 配置成人类型（`genreAdult` 增删 + `genreAdultDisabled` 禁用）
2. 搜索 TMDB 18+ 内容（NSFW 开关 → `include_adult=true`）
3. AV 搜索（磁力下载 + JAV 番号元数据）
4. 首页支持显示 AV（见第二节第 2 点 + 第三节）
> 退出里模式，以上全部隐藏。

---

## 二、2.0 导航与页面结构（v3 调整）

### 1. 底部 tab 栏：元数据 → 搜索
- 底部 tab 改为两项：**概览** · **搜索**（原「元数据」tab 移除，编辑不再是一级入口）。
- 编辑页作为**二级页**从「搜索结果」或「自定义」进入。

### 2. 首页 `page-home`（保留日期）
- 顶部**日期胶囊保留**（放弃「日期→搜索按钮」方案）。
- 概览列表顶部 **影片 / AV 胶囊 tab**（复用 `.seg`）：默认 `影片`，不记忆；`AV` tab 仅在里模式解锁后可见；`renderOverview` 按 `film.adult` 过滤；AV 卡片带「成人」角标。
- `film.adult` 自动判定：来源 JAV / TMDB `item.adult` / 类型命中 `genreAdult` 任一即 `true`，保存时 `buildFilmFromCurrent` 计算（需确认是否已存该标记）。

### 3. 搜索页 `page-search`（二级页）
- 入口：底部「搜索」tab → 进入此二级页。
- **右上角 TMDB / JAV 胶囊切换**（`meta-source-seg` 样式，居中/右对齐）。
- **搜索框**（placeholder 随源变化：TMDB「搜索影片」/ JAV「输入番号」）。
- **搜索框下方【自定义】文字按钮**：点击进入手动编辑二级页（见第四节）。
- 结果列表：TMDB / JAV 卡片复用现有样式，点击 → 编辑二级页（自动填充元数据）。

### 4. 编辑页 `page-edit`（二级页，仅表单）
- 从搜索结果点击或【自定义】进入；顶部返回 + 「元数据」标题 + 清空/保存。
- **字段按类型自适应**：影片显示影片字段，AV 显示 AV 字段（见第三节）。
- 不保留「获取元数据」入口（搜索只能从底部 tab 进）。

---

## 三、AV 与影片 元数据项差异（需落地，先列草案）

> 现有编辑表单分 basic / cast / media 三段。按类型切换展示。

**影片（movie）字段**
- basic：标题、原名、年份、评分、时长、简介、类型、国家、分级
- cast：导演、演员（姓名+角色）
- media：海报、背景图、预告片

**AV 字段（差异点）**
- basic：标题、**番号（原名）**、发行日期、时长、评分、类别（genre→成人预设）、国家（默认日本）
- cast：**女优/演员**（无角色或角色=女优）、**制作商（studio）**、**发行商（label）**、**系列（series）**、导演（可选）
- media：封面（poster）、样图（fanart，取自 sampleImages）

> 核心差异：AV 用「番号」代替原名、用「制作商/发行商/系列/女优」代替「导演/演员(带角色)」。

### 详情页展示规范（影片 / AV 共用模板）

> 虽然编辑字段分两套，但**详情页 `page-detail` 展示样式要一致**，字段尽量对应到同一位置。

- **共用同一详情模板**：标题、年份、评分行、简介、类型、演员/女优区布局完全一致，不因为 AV 而换版式。
- **字段对应**：
  - 影片「原名」↔ AV「番号」（同一位置显示）
  - 影片「演员」↔ AV「女优」
  - **AV 不显示导演**（详情页无导演行）
- **评分行下方多余字段**：AV 专属且无影片对应的字段（**制作商 ｜ 发行商 ｜ 系列**）合并成**一行**，用 `｜` 分隔，置于评分行正下方。影片无此行（或仅在有对应字段时显示）。
- 示例（AV 详情）：
  ```
  番号 ABC-123
  2024  ·  ★4.2
  制作商 S1 ｜ 发行商 正式 ｜ 系列 经典系列
  —— 简介 ——
  女优：XXX、YYY
  ```

---

## 四、自定义入口（v3 新增）

- 搜索框下方【自定义】文字按钮 → 进入**手动编辑二级页**（与搜索结果填充的编辑页是同一个 `page-edit`，但为空白手动模式）。
- 进入时默认按**影片**字段；若当前在里模式，可提供「影片 / AV」类型选择以切换字段集（具体交互待确认）。
- 「不同二级编辑页面」的理解：① 指手动模式 vs 搜索填充模式；② 或指 AV 编辑页与影片编辑页字段不同。规划采用「同一 `page-edit` + 字段按类型自适应」，避免页面爆炸。

---

## 五、当前代码真实进度（同步最新版之后）

| 任务 | 状态 |
|---|---|
| 同步最新版 | ✅ |
| JAV/javinfo 元数据搜索（API 对接 + TMDB/JAV 切换 + 字段填充） | ✅ 已加回，jsdom 测试通过 |
| 底部 tab 元数据→搜索（步骤1） | ✅ 已完成，jsdom 测试通过 |
| 搜索页独立二级页 + 右上角 TMDB/JAV 胶囊 + 【自定义】按钮（步骤2） | ✅ 已完成，jsdom 测试通过 |
| 首页 AV 胶囊 tab + `film.adult` 自动判定 + 里模式门控（步骤3） | ✅ 已完成，jsdom 测试通过 |
| AV / 影片 字段分套（步骤4） | ✅ 已完成，jsdom 32 项测试通过 |

---

## 六、实施顺序（待你下令）

1. 底部 tab 重构（元数据→搜索）+ 首页日期保留
2. 搜索页：右上角 TMDB/JAV 胶囊 + 搜索框 + 【自定义】按钮
3. 首页 AV 胶囊 tab + `film.adult` 自动判定
4. 编辑页字段按 AV/影片 自适应 + 【自定义】手动模式 + 详情页共用模板（评分行下方 `｜` 分隔多余 AV 字段）
5. 回归（复杂改动跑 jsdom；简单改动直接改完告知）

---

## 七、待确认点

1. 【自定义】进入的「不同二级编辑页」= 手动模式（同一 `page-edit` 空白）还是真的两个独立页面？建议同一页 + 类型自适应。
2. 自定义手动模式是否允许在里模式下选「AV」以切到 AV 字段？
3. AV 字段草案（第三节）是否符合预期，有无要加/减的项（如「标签」「字幕」）？
